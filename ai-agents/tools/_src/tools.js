/* 模块 4:工具调用(6 课) */
L.tools = [
['01-giving-hands','给模型装上手',5,['工具','能力边界'],
'<p>一句话：模型天生只会"说"，'+g('tool-use','工具调用')+'给它装上"做"——搜索、读写文件、调接口、跑代码，工具箱的边界就是智能体能力的边界。</p>',
'<p>大语言模型的本职是生成文本，它不能真的打开网页、真的发邮件、真的改数据库。所谓工具，就是程序 world 里的手：你把"有哪些工具、每个工具干什么、要什么参数"提前告诉模型，它干活时说"我要用搜索工具，参数是 XX"，由外部程序真正执行，再把结果递回给它看。这个"模型出主意、程序动手"的分工是理解一切智能体产品的钥匙：模型换脑不用换手，工具升级不用换脑。</p>'+ex('同一个模型，装上日历工具就是日程助理，装上数据库查询就是报表员，装上代码执行器就是数据分析师——大脑没变，手变了，工种就变了。'),
pit('别低估"工具决定能力"这句话。抱怨智能体"干不了某事"之前，先看它的工具箱里有没有那件工具、工具描述写得清不清楚——十有八九问题在手不在脑。'),
[qc('模型调用工具时，实际执行动作的是？',['模型自己','外部程序执行函数，把结果返回给模型','用户手动执行','云端自动完成'],1,'分工是"模型填单子、程序干活"：模型只输出调用意图和参数，执行永远在外部程序。')],
'<p>那"模型填单子"具体怎么填？→ <a href="#/l/tools/02-function-calling">下一课：function calling</a></p>'
],
['02-function-calling','Function Calling',5,['function calling','函数调用'],
'<p>一句话：'+g('function-calling','function calling')+'就是"模型填单子、程序干活"——模型输出一张结构化的工单（函数名+参数），程序审核执行后把结果递回去。</p>',
'<p>完整流程四拍：<b>① 报工具</b>——把工具清单（名称、功能描述、参数格式）随请求发给模型；<b>② 模型填单</b>——模型判断需要用工具时，不输出自然语言，而是输出结构化 '+g('json','JSON')+'：工具名+参数值；<b>③ 程序执行</b>——你的代码校验参数、真正调用函数；<b>④ 结果回递</b>——执行结果作为新消息进入上下文，模型看了再决定下一步。四拍可以循环多轮，直到模型认为不需要工具、直接给出最终回答。</p><p>工程上最重要的环节是 ③ 的<b>参数校验</b>：模型填的单子偶尔会缺字段、类型错、数值离谱，直接执行会出事故——先校验再执行，不合格打回重填。</p>'+ex('问"杭州下周三适合晒被子吗"：模型输出 {"tool":"get_weather","city":"杭州","date":"下周三"} → 程序真的去调天气 API → 返回"晴，紫外线中等" → 模型综合后回答"适合"。模型全程没碰网络，它只是填了单子。'),
pit('别无条件信任模型填的单子。参数是"猜"出来的：日期可能格式不对、数量可能超范围——执行前的校验层不是可选项，是必选项。'),
[qc('function calling 四拍中，工程上最不能省的是？',['工具描述写得华丽','执行前的参数校验','让模型多填几次','把结果原文全量回递'],1,'模型输出的参数是概率生成物，缺字段、超范围时有发生——先校验再执行是安全底线。')],
'<p>每个工具都自己写一套对接太累了，得有个统一标准。→ <a href="#/l/tools/03-mcp">下一课：MCP</a></p>'
],
['03-mcp','MCP：工具的 USB-C',5,['MCP','协议'],
'<p>一句话：'+g('mcp','MCP')+'是个开放协议，把"模型↔工具"的对接方式标准化——以前每接一个工具都要定制开发，现在像插 USB-C 一样即插即用。</p>',
'<p>MCP 出现前是个碎局：每个应用接每类工具都要各写一套对接代码，M×N 的组合累死所有人。MCP 把它拆成两个 M+N：工具方把自己的能力封装成<b>MCP 服务器</b>（声明"我提供哪些工具、怎么调用"），应用方实现一次<b>MCP 客户端</b>，之后任何 MCP 工具都能插进任何 MCP 应用。工具、数据源、提示词模板都可以通过它暴露，还能区分本地资源与远程服务。</p>'+ex('一个数据库的 MCP 服务器写好后，支持 MCP 的所有智能体应用都能直接用它查库——工具开发者只封装一次，应用开发者只对接一次，M×N 变成 M+N。'),
pit('别把 MCP 当成万能接口。它标准化的是"怎么连接、怎么声明"，工具本身好不好用、权限怎么管仍是各家的活——插上了 USB-C，不等于外设没病毒。'),
[qc('MCP 解决的核心问题是？',['模型思考更快','工具对接的 M×N 组合爆炸，标准化为 M+N','替代大模型','加密所有数据'],1,'统一"模型↔工具"的连接协议，工具封装一次、应用对接一次，生态从此即插即用。')],
'<p>工具多了，怎么设计才不乱？→ <a href="#/l/tools/04-tool-design">下一课：工具设计原则</a></p>'
],
['04-tool-design','工具设计原则',5,['工具设计','描述'],
'<p>一句话：给模型的工具要<b>少而清晰</b>——每个工具 description 写得像给新员工的工作交接文档，模糊的描述是模型误用工具的第一大原因。</p>',
'<p>模型选工具全靠读工具的名称和描述。描述要回答三件事：<b>什么时候用它</b>（适用场景）、<b>参数什么含义</b>（尤其单位、格式、可选值）、<b>返回什么</b>（结果长什么样）。"搜索工具"是不及格的描述；"网页搜索：当需要实时信息或用户提及你不了解的事物时使用。参数 query 为搜索关键词字符串，返回前 5 条结果标题+摘要+链接"才合格。另外三条经验：<b>合并近亲工具</b>（五个"查询不同数据源"合成一个带 source 参数的），工具太多模型会挑花眼；<b>返回结果要节制的相关</b>——只回模型需要的字段，塞原始大 JSON 纯烧 token；<b>报错要说人话</b>——"参数 date 需要YYYY-MM-DD 格式，你传了"明天""比"error 400"有用得多，模型能照着改。</p>'+ex('同一个查天气需求：拆成"查当前天气""查预报""查历史"三个工具，模型常选错；合成一个 weather(city, date_range) ，描述里写清 date_range 的两种填法，误用率立降。'),
pit('别把工具设计当成写代码的附属品。工具描述是"给模型看的文档"，它决定了模型用不用、用没用对——花在描述上的工夫回报率极高。'),
[qc('模型总是选错工具，最优先的改进是？',['换更强的模型','重写工具描述：写清适用场景、参数含义与返回格式','增加更多工具','提高温度'],1,'工具选择依赖描述文本的质量——描述模糊等于让新员工猜着干活。')],
'<p>手有了，也得有安全的地方挥舞。→ <a href="#/l/tools/05-sandbox">下一课：沙箱</a></p>'
],
['05-sandbox','执行环境与沙箱',5,['沙箱','执行环境'],
'<p>一句话：'+g('sandbox','沙箱')+'是给智能体划的试驾场地——代码随便跑、文件随便写、出了事故烧不到主系统，这是"放手让它干活"的前提。</p>',
'<p>智能体常见的动作里，执行代码和写文件是高危的：模型生成的代码可能有死循环、可能误删文件、可能访问不该访问的东西。沙箱的思路是<b>隔离 + 限额</b>：单独的容器或虚拟机里跑，文件系统独立、网络可关、CPU/内存/时长设上限，跑完销毁或回收。配套的还有"'+g('idempotency','幂等性')+'"设计：让写操作支持重复执行而不产生叠加效果，这样失败重试才安全。注意沙箱不只防"恶意"，更多时候防的是"无心之失"——模型没有坏心，但会犯错。</p>'+ex('编码智能体的标准姿势：在一个一次性容器里跑测试、装依赖，跑挂了直接销毁重来，主机的环境永远干净；跑代码设 60 秒超时，死循环到点自动掐。'),
pit('别用"它平时挺靠谱"代替沙箱。平时靠谱和每次都靠谱是两回事，概率性输出意味着第 1000 次也可能出错——隔离与限额是给那一次准备的。'),
[qc('智能体执行模型生成的代码，为什么要放沙箱里？',['跑得更快','隔离限额：死循环、误删文件等意外烧不到主系统','节省 token','方便调试'],1,'概率性输出意味着偶发的坏代码不可避免，沙箱的隔离与限额把爆炸半径限制在场地内。')],
'<p>最后把多个工具串起来用。→ <a href="#/l/tools/06-multi-tool">下一课：多工具编排</a></p>'
],
['06-multi-tool','多工具编排',5,['编排','重试'],
'<p>一句话：真实任务很少一工具搞定——<b>查了再算、算了再写、失败重来</b>，把多个工具按依赖关系串并联，才是智能体的日常。</p>',
'<p>编排要处理三件事。<b>依赖关系</b>：先查汇率才能换算、先有草稿才能审稿——有依赖的串行执行，没依赖的并行执行（同时查三个数据源），并行能大幅省时间。<b>失败处理</b>：网络抖一下就整个任务报废不划算，标准做法是'+g('retry-backoff','重试与退避')+'——立刻重试一次，还不行等 2 秒、再不行等 8 秒，指数退避；重试三次仍失败就降级（用缓存数据/报告部分完成）而不是硬崩。<b>超时与止损</b>：每个工具调用设超时，整个任务设步数上限，防止"无限重试"烧钱烧电。</p>'+ex('"生成汇率日报"：并行调用三个 API 拉数据（省时间）→ 汇总计算 → 生成图表 → 发邮件。第三步失败时自动重试两次，仍失败则退回"只发文本版"——日报照样出，只是丑一点。'),
pit('别让"重试"变成"无限重试"。没有上限的重试遇到持续性故障（服务下线）就是自杀式循环——重试要有次数上限，重试耗尽要有降级出路。'),
[qc('工具调用连续失败，工程上的标准处理链是？',['无限重试直到成功','立即重试→指数退避重试→耗尽后降级或报错','直接放弃整个任务','换一个模型再试'],1,'退避重试应对瞬时故障，重试耗尽后优雅降级保证任务有产出——无限重试是事故温床。')],
'<p>单兵装备的最后两件进阶：先让它的嘴变规矩。→ <a href="#/l/tools/07-structured-output">下一课：结构化输出</a></p>'
],
['07-structured-output','结构化输出与 JSON Mode',5,['结构化输出','JSON'],
'<p>一句话：智能体的输出要进下游程序，就不能是随性散文——'+g('structured-output','结构化输出')+'让模型按预定 '+g('schema','schema')+' 交卷，'+g('json-mode','JSON Mode')+'则从生成层面强制合法 JSON，两者合起来是智能体管道化的地基。</p>',
'<p>为什么要死磕格式？智能体的输出经常要被程序接走：审稿的问题清单要进工单系统、抽取的字段要入库、工具调用的参数要被执行器解析——格式漂一点，下游就崩。三个层次的约束由弱到强：<b>① 提示词里给模板</b>（"输出如下 JSON 结构…"），便宜但偶尔跑偏；<b>② schema 约束</b>（把字段定义随请求传给 API，由服务商校验或重试），可靠性大增；<b>③ 受限解码</b>（JSON Mode / 结构化输出模式，从生成机制上只允许产出合法 JSON），最强约束。配套习惯：<b>温度调低 + 结构化输出</b>是稳定管道的标配组合；schema 里给字段写清楚 description——模型也是照着字段说明填表的。</p>'+ex('发票抽取流水线：schema 定义 invoice_number/date/amount/seller 四个字段及类型 → 服务商 schema 约束强制合法 JSON → 下游程序直接入库。没上约束前，偶尔蹦出一句话解释前缀导致解析崩溃；上约束后三个月零格式事故。'),
pit('别以为"合法 JSON"就等于"内容正确"。JSON Mode 保证的是语法合法，字段填得对不对、值是否 hallucinated 它管不着——语法靠约束，内容靠校验，两道关都要过（下一课讲内容层）。'),
[qc('JSON Mode 解决的是哪个问题？',['字段内容一定正确','输出语法合法：从生成机制上保证产出可解析的 JSON','速度更快','成本更低'],1,'JSON Mode 管语法不管语义——合法 JSON 里的字段值仍可能错，内容校验是另一道关。')],
'<p>语法锁住了，内容错了怎么办？→ <a href="#/l/tools/08-parse-retry">下一课：解析失败与重试</a></p>'
],
['08-parse-retry','解析失败与重试工程',5,['解析','重试'],
'<p>一句话：拿到模型输出只是第一步——<b>校验、修复、重试、降级</b>四层兜底，才能让"偶尔抽风"的概率性输出变成生产级管道。</p>',
'<p>四层逐个上。<b>① 校验层</b>：解析前先验——是合法 JSON 吗？必填字段都在吗？类型对吗？数值在合理范围内吗？（金额是负数、日期是 2 月 30 日，语法没错内容错了。）<b>② 自动修复层</b>：常见小病自动治——输出裹了一层 Markdown 代码块标记就剥掉、单双引号混用就规范化、截断的补个收尾括号。<b>③ 重试层</b>：修不好就把<b>报错信息原文</b>喂回去让它改——"字段 amount 缺失，price 有但不是数字，请按 schema 重新输出"，带着具体错误的定向重试，成功率远高于原样重发。<b>④ 降级层</b>：重试两三次仍失败，别死磕——走人工队列、用默认值、标记该条待处理，让流水线继续跑。<br><br>工程原则：<b>每一层都要打日志</b>——哪类错误最多、重试几次能救回来，这些数据告诉你该在哪一层加投入（错误集中在某字段，先改 schema 或提示词，比重试更治本）。</p>'+ex('入库流水线的解析日志发现：70% 解析失败集中在 date 字段（模型爱输出"2025年3月"格式）。修复不是加重试，而是在 schema 里给 date 写清"YYYY-MM-DD，月份两位"并给示例——错误源头消失，重试率降到 2%。'),
pit('别用"无限重试"掩盖设计问题。重试烧的是真金白银，同一个字段天天靠重试救回来，说明 schema 或提示词有病——重试是安全网，不是治疗方案。'),
[qc('定向重试时，最该随重试一起发回给模型的是？',['更高的温度','报错信息原文：哪个字段错了、怎么错的，让它定向修正','原任务重发一遍','更长的提示词'],1,'带着具体错误的定向重试，等于把"猜错原因"变成"看着错题改卷"——成功率远高于盲目重发。')],
'<p>单兵装备齐了，给它配上眼睛和耳朵。→ <a href="#/l/multimodal/01-multimodal-input">下一模块：多模态</a></p>'
]
];
