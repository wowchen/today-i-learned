/* features/04-scaling (自动生成) */
ML.registerLesson({"id":"features/04-scaling","module":"features","order":4,"title":"归一化与标准化：统一量纲","minutes":6,"keywords":["归一化","标准化","量纲"],"concept":"<p>一句话：<gd data-term=\"normalization\">归一化</gd>和<gd data-term=\"standardization\">标准化</gd>都是\"把不同量纲的特征拉到同一水平\"，<b>避免数值大的特征在计算中压倒数值小的特征。</b></p>","core":"<p>先看问题：假设两个特征，年龄（18-80）和年收入（3 万-300 万）。很多算法要靠\"距离\"或\"数值大小\"来判断谁更重要，那么收入这个百万级的数字会把年龄彻底淹没——模型实际上只看了收入。<b>这不是模型的问题，是数据没处理好。</b></p><p>两种常见做法：<br><b>归一化</b>：把数据压到 0 到 1 之间（用最小值减、用极差除）。适合取值范围有明确边界的场景，但对极端值敏感。<br><b>标准化</b>：把数据变成\"均值 0、标准差 1\"。更常用，对异常值的抵抗力也更好一些。</p><p>哪些模型需要做？<b>靠距离或梯度计算的模型需要</b>：K 近邻、支持向量机、K 均值聚类、神经网络、带正则化的线性模型。哪些不需要？<b>树模型基本不需要</b>——决策树只看\"某个阈值切一刀\"，数据怎么缩放不影响切分结果。<b>所以\"要不要归一化\"取决于你用的是什么模型。</b></p><div class=\"ex\">一个必须记住的操作顺序：<b>缩放参数只能用训练集算出来，再套用到验证集和测试集。</b>如果用了全部数据来算均值和极差，验证集的缩放里就混进了测试集的信息，评估会虚高——这是最隐蔽的数据泄漏之一，因为不报错，成绩还更好看。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别忘了\"预测时\"也要走同一套缩放。如果训练时把年龄缩放到 0-1 之间，上线后来了一个 45 岁的新样本，也必须用<b>训练时算出的</b>最小值和极差去换算，而不是用新样本自己的。这就是为什么缩放必须写进<gd data-term=\"pipeline\">流水线</gd>、而不是手动在数据表上做一遍。</div>","quiz":[{"type":"choice","q":"下列哪种模型最不需要做特征归一化？","options":["K 近邻","支持向量机","决策树","神经网络"],"answer":2,"explain":"树模型基于阈值切分，特征缩放不改变切分点的相对顺序，因此对量纲不敏感。而依赖距离或梯度的模型则必须统一量纲。","source":"想一想"}],"links":"<p>特征处理得差不多了，但列不是越多越好。→ <a href=\"#/l/features/05-selection\">下一课：特征选择</a></p>"});
