/* neural/03-backprop (自动生成) */
ML.registerLesson({"id":"neural/03-backprop","module":"neural","order":3,"title":"反向传播：倒查责任","minutes":7,"keywords":["反向传播","梯度","责任分配"],"concept":"<p>一句话：<gd data-term=\"backprop\">反向传播</gd>是训练神经网络的核心算法——<b>先算出最终结果错了多少，再沿着网络一层层往回追：每个参数该为这个错误负多少责任，然后按责任大小去调整。</b></p>","core":"<p>过程分两趟：<br><b>前向</b>：数据从输入层进来，一层层算到输出，得到预测结果，和真实值比较算出误差。<br><b>反向</b>：从误差出发往回推，算出\"每个权重稍微变一点，误差会变大还是变小、变多少\"（这就是梯度），然后按照让误差变小的方向去调整权重。</p><p>打个比方：一个项目最终没达标，要追责到每个人——不是平均分摊，而是看\"如果这个人多做一点，结果会好多少\"。贡献大的多调一点，没什么影响的就少动。<b>反向传播干的就是这件事，只不过\"追责\"是用数学严格算出来的。</b></p><p>它的巧妙之处在于效率：网络可能有几千万个参数，但反向传播一趟就能算出所有参数的调整方向，不需要每个参数都单独试一遍（那样根本算不完）。<b>这是深度学习能真正跑起来的关键前提。</b></p><div class=\"ex\">一个常见现象可以在这里得到解释：层数太深时，靠前的层往往学得慢，因为责任在往回传的路上被层层稀释——这叫\"梯度消失\"。这也是为什么会有残差连接、批量归一化这类技巧，它们都是为了给梯度修一条更通畅的回传路径。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别以为训练一次就没问题。反向传播只是\"算方向\"，真正走多少步、走多久，取决于<gd data-term=\"learning-rate\">学习率</gd>和训练轮数。学习率太大训练会震荡甚至发散，太小则慢如蜗牛——这个旋钮的调整，是实践中绕不开的一步。</div>","quiz":[{"type":"choice","q":"反向传播的核心目的是？","options":["加快前向计算速度","高效地计算出每个参数对最终误差的影响，从而知道该往哪个方向调整","减少网络的层数","自动生成训练数据"],"answer":1,"explain":"它通过一次反向遍历，把所有参数的梯度一次性算出来，这是它能支撑超大规模参数训练的关键。","source":"想一想"}],"links":"<p>通用的网络讲完了，接下来看三个为特定数据类型而生的结构。→ <a href=\"#/l/neural/04-cnn\">下一课：看图的行家</a></p>"});
