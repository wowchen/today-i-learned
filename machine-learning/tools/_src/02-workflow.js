/* 模块 2:建模全流程(6 课) */
L.workflow = [
['01-define','第一步：把问题定义清楚',6,['问题定义','目标指标','可行性'],
'<p>一句话：建模的第一个动作不是打开数据，而是把"要解决什么"写成一句能被验证的话——<b>预测谁、预测什么、什么时间点预测、预测错了会怎样。</b></p>',
'<p>一句合格的题目长这样："在用户注册后第 7 天，预测他未来 30 天内是否会流失。"它把四件事说死了：对象（注册满 7 天的用户）、结果（是否流失）、预测时点（第 7 天）、时间窗（未来 30 天）。<b>少任何一样，后面全乱。</b></p><p>定义时点尤其关键。如果说"预测用户会不会流失"却不限定"在第几天预测"，数据里就会混进那些早就流失、后来又回来的用户，模型学到的东西毫无意义。业务上真正能用的模型，前提是"在需要做决策的那一刻，我手上有什么信息"。</p><p>还要提前想明白：<b>预测错了代价是什么。</b>把要流失的客户判成不流失（漏判）和把好客户误判成流失（误判），业务损失往往天差地别，而这直接决定了后面该用哪个评估指标、把阈值定在哪。</p>'+ex('一个常见错误：题目写成"提升客户满意度"。这没法建模——满意度怎么量化？改成"预测未来 30 天内客户是否会取消订阅"，标签可观测、时点可确定，才是一个能开工的题目。<b>把模糊愿望翻译成可测量的问题，是建模者最值钱的能力。</b>'),
pit('别跳过这一步直接跑模型。方向错了，模型再准也是错的——预测了一个没人会用的结果，或者用了一个决策时根本拿不到的信息。项目返工十有八九都是死在这一步。'),
[qc('下列哪个题目最适合直接开工建模？',['预测客户会不会喜欢我们的新功能','在订单创建后 5 分钟内，预测该订单是否会因欺诈被拒付','提升平台整体交易体验','分析用户行为数据'],1,'它有明确对象、明确时点、可观测的结果标签。"喜欢""体验"这类词无法量化，不能直接建模。')],
'<p>题目定了，下一个动作是切数据。→ <a href="#/l/workflow/02-data-split">下一课：为什么要切三份</a></p>'
],
['02-data-split','训练、验证、测试：为什么要切三份',6,['数据集','划分','测试集'],
'<p>一句话：'+g('dataset','数据集')+'必须切成三份——训练集用来学，'+g('validation','验证集')+'用来调，测试集只在最后用一次——<b>因为"自己考自己"的分数毫无意义。</b></p>',
'<p>为什么不能只切两份？因为调参过程本身会"偷看"验证集。你反复试了二十种设置，每次都看验证集成绩来挑最好的，那验证集就已经被间接用进训练了，它的分数会偏乐观。<b>所以需要一个从头到尾没被任何人看过、只在最终验收时开一次的测试集。</b></p><p>常见比例是 6:2:2 或 7:1.5:1.5。数据量很大时，验证集和测试集占比可以更小（几万条足够说明问题）；数据量很小（几百条）时，就该上'+g('cross-validation','交叉验证')+'，让每一条数据都轮流当过一次验证集。</p><p>三种数据的角色可以这样记：训练集是习题册，验证集是模拟考，测试集是高考。<b>模拟考可以考很多次、用来调整复习策略，高考只有一次，考完不许再改。</b></p>'+ex('一个必须警惕的做法：数据集切分只有两种正确姿势。<b>随机切分</b>适用于样本彼此独立的场景（比如一批用户）；<b>按时间切分</b>适用于一切有时间顺序的场景（预测未来，就绝不能拿未来的数据训练）。股票、销量、用户行为这类数据，用随机切分会造成严重的"用未来预测过去"，成绩虚高到离谱。'),
pit('别在测试集上反复调参。测试集一旦被你用来看成绩、改方案，它就不再是测试集了，它变成了第二个验证集。真正的做法是：测试集开一次，无论结果好坏都照实报告。'),
[qc('预测"下个月设备是否会故障"，把 2024-2026 年的数据随机打乱后切分训练集和测试集，最大的问题是什么？',['样本量不够','随机切分让模型用未来数据预测过去，成绩会虚高','应该只切两份','应该用更多特征'],1,'时序场景必须按时间切分：训练集全部在测试集之前。随机切分等于让模型偷看未来，这是最隐蔽也最致命的评估错误之一。')],
'<p>数据切好了，动手训模型之前，还有一件最容易被跳过的事。→ <a href="#/l/workflow/03-baseline">下一课：先立一个笨基线</a></p>'
],
['03-baseline','先立一个笨基线',5,['基线','对照','改进'],
'<p>一句话：'+g('baseline','基线')+'是一个"最笨但一定跑得通"的方案——<b>没有基线，你根本不知道模型 85 分是优秀还是不及格。</b></p>',
'<p>基线怎么做？挑一个简单到不用思考的办法：分类问题全猜"多数类"（比如"所有用户都不会流失"），或者用一条业务规则；回归问题直接拿"上一次的值"或"历史平均值"当预测。<b>它不需要聪明，需要的是可靠、可复现、可解释。</b></p><p>有了基线，模型的价值才可量化。假设设备故障预测，全猜"不故障"的基线准确率是 96%，你的模型 96.5%——听起来还行？其实几乎没学到东西。反过来，某个场景基线只有 60%，模型做到 75%，那才是真本事。<b>脱离基线谈分数，是在自欺。</b></p><p>基线还有一个隐形好处：它是最快的"数据体检"。如果模型跑不过一个笨基线，八成不是算法不行，而是数据切分错了、标签有问题、或者特征里混了噪声——这些毛病在复杂模型里会被掩盖，在基线面前却藏不住。</p>'+ex('基线还能当"省钱开关"。如果花三个月做的复杂模型只比基线好 1 个百分点，而业务上这 1 个点换不来任何决策变化，那就该老老实实上基线，把人力投到别处。<b>能用简单方案解决的，就不要复杂方案——这是一种专业素养，不是偷懒。</b>'),
pit('别拿"和上一版模型比"当基线。同类模型之间比进步，只能说明你在调优；能不能打赢一个零成本的笨办法，才说明这件事值不值得做。'),
[qc('某流失预测模型准确率 93%，但数据中"未流失"用户占 94%。最该做的判断是？',['93% 已经很好，可以上线','模型还不如"全猜未流失"的基线，需要换评估指标重看','应该增加训练轮数','应该换成神经网络'],1,'全猜多数类的基线准确率就是 94%，模型 93% 反而更差。这类不平衡问题必须看召回率、精确率或 AUC。')],
'<p>基线立好，终于可以开始训练了——但请从最简单的模型起步。→ <a href="#/l/workflow/04-iterate">下一课：训练与迭代</a></p>'
],
['04-iterate','训练与迭代：从简单开始',6,['迭代','调参','简单模型'],
'<p>一句话：建模是个循环，不是一条直线——<b>先跑通最简单的流程，再一步步加复杂度，每一步都用验证集确认"这次改动真的有用"。</b></p>',
'<p>一个务实的迭代顺序：<br>第一步，把'+g('pipeline','流水线')+'从头到尾跑通一遍，哪怕用的是最粗糙的特征、最简单的模型，先让整条链路能出结果。<br>第二步，看错在哪——把模型判错的样本捞出来一条条看，往往比看任何指标都有启发。<br>第三步，针对性地改：补特征、清数据、换思路。<br>第四步，用验证集确认这次改动带来了多少提升，没提升就退回去。<b>一次只改一处，才知道是哪一处起了作用。</b></p><p>为什么要从简单模型开始？因为简单模型跑得快、看得懂、方便定位问题。逻辑回归的特征权重可以直接看，决策树能画出判断路径。等你确认"数据和特征都没问题"了，再上复杂模型压榨最后几个百分点也不迟。</p>'+ex('一个典型场景：某团队直接上梯度提升树，成绩不理想，折腾两周无果。回头换成逻辑回归，发现某个特征权重异常大——原来这列里混进了"未来才会产生的信息"。修掉之后，简单模型立刻反超原来的复杂模型。<b>复杂模型掩盖了数据问题，简单模型把它暴露出来。</b>'),
pit('别在一开始就追求极致分数。模型上线后用不上、维护不起，分数再高也是负担。同时警惕"改到动不了了才想起来看数据"——<b>至少一半的提升来自数据质量，而不是算法调参。</b>'),
[qc('第一次建模，验证集成绩不理想。最专业的第一步是？',['直接换成最先进的模型','把超参数都调一遍','把判错的样本捞出来逐条看，找出问题模式','增加数据量'],2,'先诊断再治疗。逐条看错例能直接暴露数据、标签或特征的问题，这比盲目换模型有效得多。')],
'<p>模型调得差不多了，怎么算"够用"？→ <a href="#/l/workflow/05-accept">下一课：验收标准</a></p>'
],
['05-accept','验收：模型要多好才算够',5,['验收','业务指标','阈值'],
'<p>一句话：模型好不好不看分数绝对值，看<b>它能不能带来业务上的改变</b>——验收标准应该在建模之前就定下来，而不是等分数出来再回头挑一个好看的指标。</p>',
'<p>定验收标准时要回答三个问题：<br><b>一、跟谁比？</b>比基线好多少才算合格。是"至少打赢基线 5 个百分点"，还是"和现有规则方案持平即可"。<br><b>二、错在哪边更要紧？</b>如果"漏判"的代价远大于"误判"，就选召回率优先的阈值；反之亦然。这个取舍只有业务方说得清，算法工程师替不了。<br><b>三、用在哪一档？</b>模型是辅助人工（人可以复核，容错高），还是全自动执行（错了没人兜底，要求高得多）？</p><p>另外，验收还要看"分数之外"的东西：模型跑一次要多久、能不能嵌进现有系统、结果能不能解释给业务听、维护成本多高。一个 AUC 0.95 但需要十台服务器、每次出结果要等两小时、还说不清理由的模型，在多数业务里都活不下去。</p>'+ex('小技巧：把验收标准写成"模型上线后，把某个人工动作的时间从 X 降到 Y"或者"把某类漏损的发现率提升 Z%"，比写"准确率达到 90%"有用得多。<b>前者是业务语言，后者是技术语言——预算和资源永远跟着业务语言走。</b>'),
pit('别等到模型做完再去问业务"这个分数行不行"。那时候你已经投入了大量精力，会不自觉地替自己的结果辩护。验收标准提前定、白纸黑字写下来，是保护自己的做法。'),
[qc('业务方说"漏判一个真流失客户损失 5 万元，误判一个非流失客户只损失 200 元"。调阈值时应该偏向？',['提高召回率，宁可误判也别漏判','提高精确率，宁可漏判也别误判','两边一样重要','不用调阈值'],0,'漏判代价远大于误判，就该放宽判定条件、提高召回率。阈值的选择本质是成本比较，不是技术问题。')],
'<p>验收通过只是开始，模型上线后有自己的命。→ <a href="#/l/workflow/06-launch">下一课：上线与监控</a></p>'
],
['06-launch','上线与监控：模型是活的',6,['上线','监控','数据漂移'],
'<p>一句话：模型上线不是终点而是起点——<b>真实世界一直在变，准确率会自己往下掉，'+g('mlops','MLOps')+' 存在的意义就是让模型能被持续看着、随时更新。</b></p>',
'<p>上线通道一般有两关。第一关是'+g('ab-online','在线实验')+'：先给小部分流量（比如 5%）用模型，其余照旧，对比两组的关键指标，确认模型真的有用再放量。第二关是持续监控，盯三样东西：<br><b>输入有没有变</b>（'+g('drift','数据漂移')+'）——用户的年龄结构变了、渠道变了、设备型号变了，模型的输入分布和训练时不同了；<br><b>输出有没有变</b>——预测出的"会流失人数"突然翻了三倍，往往是数据管道出了问题，而不是用户突然集体要跑；<br><b>业务指标有没有变</b>——最根本的一条，模型上线后那个业务数字到底动了没有。</p><p>另外别忽略"人机接口"的设计：模型给出高风险的判断时，是自动执行还是转人工复核？一线员工看到的是一条"高风险"结论，还是能看到理由和置信度？这些设计决定了模型在真实工作流里是被信任还是被绕过。</p>'+ex('一个真实的教训类型：某模型上线半年表现稳定，第七个月突然失效。原因不是模型退化，而是上游业务改了一次字段含义——原本"已登录"字段的含义变了，特征全歪了。<b>监控输入分布，能在业务指标恶化之前就报警。</b>'),
pit('别指望"训练一次，永久使用"。哪怕数据分布没变，人也在适应模型（比如运营会针对模型规则调整动作），模型的有效期通常远比想象中短。一个负责任的方案，从第一天起就要写清"多久重训一次、谁来负责盯"。'),
[qc('模型上线三个月后性能明显下滑，最应该先排查什么？',['算法是不是选错了','输入数据的分布是否发生了漂移','训练时用的服务器配置','样本量是否够大'],1,'已上线且初期正常的模型，性能下滑最常见的原因是输入分布漂移或数据管道变动，而不是算法选错。先查数据，别急着换算法。')],
'<p>流程走完了，接下来逐个拆开算法。先看最基础的回归。→ <a href="#/m/regression">下一模块：回归——预测数值</a></p>'
]
];
