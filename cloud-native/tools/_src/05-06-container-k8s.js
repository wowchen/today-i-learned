/* 模块 5:容器技术(7 课) */
L.container = [
['01-why','容器解决什么问题',6,['容器','环境一致','依赖'],
'<p>一句话：'+g('container','容器')+'把应用连它需要的运行环境和依赖一起打包成一个标准单元，保证"在我这儿能跑，在你那儿也能跑"。</p>',
'<p>开发最经典的抱怨是"我这里明明是好的"。原因通常是环境不一致：机器上装的 JDK 版本不同、缺了某个系统库、环境变量不一样、文件路径不一样。虚拟机能解决一半——整台机器都复制过去，但太重：几百 MB 起步、启动要几十秒，为了跑一个小服务显得夸张。</p><p>容器的思路是只打包<b>应用与用户态依赖</b>，共用宿主机内核。于是镜像可以小到几十 MB，启动在秒级，一台机器上能跑几十上百个。它带来的直接收益是<b>交付方式统一</b>：构建一次，测试、预发、生产用同一个产物，环境差异被消灭在镜像里。</p>'+ex('同一个镜像在开发笔记本、测试环境、生产集群里跑出来的行为完全一致，因为三处的运行时、依赖、配置路径都来自同一个打包结果——"环境问题"从口头禅变成了构建阶段的一次性工作。'),
pit('别把容器当"小虚拟机"用。容器里不该装 sshd、不该跑 systemd、不该把多个互不相关的进程塞进去。一个容器一个进程是原则，硬塞多进程会让日志、启停、健康检查全乱套。'),
[qc('容器相比虚拟机最本质的区别是？',['容器更快更小，因为它共享宿主机内核而不是模拟整套硬件','容器不需要操作系统','容器更安全','容器只能跑 Linux 应用'],1,'共享内核让容器轻量，也带来与虚拟机不同的隔离边界与安全考量。')],
'<p>那它是靠什么做到互不干扰的？→ <a href="#/l/container/02-isolation">下一课：namespace 与 cgroup</a></p>'
],
['02-isolation','namespace 与 cgroup：隔离的真相',6,['namespace','cgroup','隔离','限额'],
'<p>一句话：'+g('ns-cgroup','容器隔离')+'不靠虚拟化，靠 Linux 内核两样东西——namespace 让进程"看不见别人"，cgroup 让进程"用不过额度"。</p>',
'<p><b>namespace</b> 是一层视野隔离：进程、网络、文件系统挂载点、主机名、用户 ID、进程间通信都有各自的命名空间。容器里的进程看到的是自己那套 PID 编号（1 号进程就是应用本身）、自己的网卡与 IP、自己的根目录，看不到宿主机上的其他进程。<b>cgroup</b> 则是资源限额：限制能用多少 CPU、多少内存、多少磁盘 IO，超了就排队或被终止（内存超限常见表现是被 OOM 杀掉）。</p><p>理解这两点能解释很多现象：容器里 <code>top</code> 显示的 CPU 是宿主机的；容器内存超限被杀不是"程序 bug"而是配额；容器里改的 hosts 文件重启就没了。</p>'+ex('一个 Java 容器只给了 2G 内存限制，进程却按宿主机 64G 去算堆大小，结果频繁被 OOM 杀掉。修正是让 JVM 按容器可见的内存来配置堆——这是容器化最常见的坑之一。'),
pit('别在容器里用"按宿主机资源"的方式调优。JVM、数据库、Node 等运行时会读系统总内存与核数，容器环境下必须显式告知限制值，否则会出现"看起来配置很大、实际一直被限"的怪象。'),
[qc('容器内存超限通常会发生什么？',['自动扩容','进程被内核 OOM 终止','性能自动降级但不影响','宿主机重启'],1,'cgroup 达到内存上限时，内核会终止超额进程，表现为容器反复重启。')],
'<p>接下来看它怎么被打包出来的。→ <a href="#/l/container/03-image-layer">下一课：镜像与分层</a></p>'
],
['03-image-layer','镜像与分层',6,['镜像分层','只读层','可写层','复用'],
'<p>一句话：'+g('image-layer','镜像')+'由一层层只读层叠加而成，容器启动时在最上面加一层可写层——公共部分被复用，所以镜像能做得又小又快。</p>',
'<p>一个典型镜像自下而上是：基础系统层（如精简的 Linux）、运行时层（JDK 或 Python）、依赖层、应用代码层。每层只记录与上一层的差异，多台机器、多个镜像之间可以共享相同的底层，本地存储与传输量都大幅减少。容器运行时只把最上面的可写层交给容器，下面全部只读共享。</p><p>这个设计带来两个重要推论：一是<b>层次顺序影响构建速度</b>——变化少的层放下面，改动频繁的代码放上面，能最大化命中缓存；二是容器<b>删除重建后数据会丢</b>，因为可写层随容器生命周期消失，持久数据必须挂载外部存储。</p>'+ex('把依赖安装与应用代码分成两层：改代码时只重建最后一层，几秒钟完成；如果写成一行同时装依赖和拷代码，每次改代码都要重装依赖，构建从几秒变成几分钟。'),
pit('别在镜像里写入运行时数据。日志、上传文件、临时数据库都写进可写层的话，容器一重启全没了，还会把镜像层撑大。这些内容要么进日志系统，要么挂持久卷或对象存储。'),
[qc('镜像分层的最大好处是？',['降低安全性要求','层可复用、构建与分发都更快更省','让容器跑得更快','减少操作系统依赖'],1,'共享只读层让存储与传输成本大幅下降，也让缓存命中成为可能。')],
'<p>分层是怎么被写出来的？→ <a href="#/l/container/04-dockerfile">下一课：Dockerfile</a></p>'
],
['04-dockerfile','Dockerfile 与构建',7,['dockerfile','构建','多阶段'],
'<p>一句话：'+g('dockerfile','Dockerfile')+'是一份文本文配方，按顺序描述"从哪个基础镜像开始、装什么、拷什么、怎么启动"，构建工具据此产出镜像。</p>',
'<p>几条被反复验证的写法：<b>选精简基础镜像</b>（够用就好，越小的镜像拉取越快、漏洞越少）；<b>先装依赖再拷代码</b>（利用层缓存）；<b>用多阶段构建</b>（编译阶段用带完整工具链的大镜像，最终产物只拷编译结果到运行镜像，体积可能从 1G 降到 100M）；<b>不要用 latest 标签</b>（版本漂移会让"昨天能跑今天不能跑"）。</p><p>还有安全底线：容器默认不该以 root 运行，构建时创建普通用户并切换；不要把密钥、令牌写进镜像或作为构建参数——镜像层可以被任何拿到镜像的人解出来。</p>'+ex('一个 Java 服务的两阶段构建：第一阶段用完整 JDK 镜像编译打包，第二阶段只用精简运行环境加一个 jar 文件，最终镜像从 900MB 降到 120MB，拉取时间从几分钟变成十几秒。'),
pit('别把密码、token 写进 Dockerfile 或镜像里，哪怕后面删掉也没用——镜像层是可回溯的，删掉只是在新层标记删除，旧层里还留着。敏感信息要用运行时注入或密钥管理服务。'),
[qc('多阶段构建的主要收益是？',['构建更快','最终镜像更小、运行时更干净','更安全','不用写 Dockerfile'],1,'把编译工具链留在构建阶段，只把产物带进运行镜像，体积与攻击面都下降。')],
'<p>镜像做好之后放哪？→ <a href="#/l/container/05-registry">下一课：镜像仓库</a></p>'
],
['05-registry','镜像仓库与分发',5,['registry','镜像仓库','标签','分发'],
'<p>一句话：'+g('registry','镜像仓库')+'是集中存放与分发镜像的地方，用"仓库名 + 标签"定位具体版本，集群每一台机器都从这里拉取。</p>',
'<p>它的作用类似代码仓库，但存的是构建产物。命名通常包含注册地址、项目、镜像名与标签，例如 registry.example.com/pay/service:v1.2.3。标签策略是重点：<b>用不可变的版本号或提交号</b>（v1.2.3、git 短哈希）而不要用 latest 或 "test" 这类会漂移的标签；生产环境甚至可以按<b>镜像摘要</b>部署，确保拉到的字节完全一致。</p><p>仓库分公共与私有：公共镜像方便取用基础镜像，但企业内部代码必须放私有仓库，并配好登录凭据与拉取权限。大集群还要考虑就近拉取、镜像预热与清理策略，否则"镜像拉不下来"会成为发布失败的常见原因。</p>'+ex('发布失败排查的一个常见分支：代码全对，但目标节点拉镜像超时或认证失败。把镜像仓库的连通性与凭据加入发布前检查清单，能省掉大量深夜排查。'),
pit('别在生产环境用 latest 标签。更危险的是"用同一个标签覆盖推送新内容"——同一份配置在不同时间部署出不同版本，回滚时也不知道该回到哪一版。标签要么不可变，要么别用在生产。'),
[qc('生产部署推荐用什么标识镜像版本？',['latest','同名的可覆盖标签','不可变的版本号或提交哈希','构建时的日期随便写'],2,'不可变标签让"同一版本部署出的结果一致"，也让回滚有明确目标。')],
'<p>仓库里的镜像，是谁把它跑起来的？→ <a href="#/l/container/06-runtime">下一课：容器运行时</a></p>'
],
['06-runtime','容器运行时：真正干活的那一层',6,['containerd','运行时','cri'],
'<p>一句话：'+g('container-runtime','容器运行时')+'是真正负责拉镜像、建命名空间、限资源、启动进程的底层程序，如 containerd、CRI-O；我们敲的命令只是它的外壳。</p>',
'<p>层次大致是：命令行工具（docker、nerdctl）→ 高层运行时（containerd）→ 底层运行时（runc 之类，真正调用内核创建容器）。Kubernetes 通过 CRI 这个标准接口与运行时对接，所以集群可以用 containerd，也可以用别的实现，而不必绑定某一家。</p><p>理解这一层对排障很有用：容器起不来时错误来自哪个环节很重要——拉镜像失败是仓库与网络问题，创建命名空间失败是权限或内核参数问题，进程退出是应用自身问题。知道链路，才能定位到正确的日志。</p>'+ex('Kubernetes 从某个版本后不再内置 docker 作为运行时，很多团队遇到"节点升级后容器起不来"，根因就是运行时换了、镜像存储与命名空间都变了，需要重新对齐配置。'),
pit('别把"容器运行时"和"镜像格式"混为一谈。运行时可以换，镜像格式是标准（OCI）。正因为有这两个标准，容器生态才能既不锁定厂商、又保持互操作。'),
[qc('Kubernetes 通过什么标准接口对接不同的容器运行时？',['OCI','CRI','HTTP','SSH'],1,'CRI 是容器运行时接口，让编排层与具体运行时解耦。')],
'<p>绕了一圈，回到最实际的选型问题。→ <a href="#/l/container/07-container-vs-vm">下一课：容器 vs 虚拟机</a></p>'
],
['07-container-vs-vm','容器 vs 虚拟机：怎么选',6,['选型','隔离强度','启动速度'],
'<p>一句话：虚拟机隔离强、启动慢、开销大；容器轻、快、密度高，但隔离弱一些——两者不是替代关系，而是各自解决不同问题，生产上常常一起用。</p>',
'<p>对比几个维度就清楚了：<b>隔离强度</b>——虚拟机有独立内核，容器共享内核，因此多租户强隔离场景虚拟机更稳；<b>启动与密度</b>——容器秒级启动、单机可跑上百个，虚拟机通常几十秒、单机十几个；<b>可移植性</b>——镜像一致性好，容器明显占优；<b>运行有状态的重型组件</b>——数据库、需要特殊内核参数的负载，虚拟机或裸金属依然常见。</p><p>所以云上的主流形态是<b>虚拟机里跑容器</b>：用虚拟机的强隔离做安全边界与资源边界，用容器做应用交付与弹性调度。这也是各大云容器服务的底层实现方式。</p>'+ex('一个真实的组合：节点用虚拟机保证隔离与稳定的资源配额，上面用 Kubernetes 调度几十个容器化的微服务。运维升级只换节点，业务在容器层滚动发布，两边收益都拿到。'),
pit('别为了"先进"把有状态的核心数据库硬塞进容器。数据库对磁盘性能、内存稳定性、网络延迟都敏感，容器编排带来的调度抖动往往得不偿失。有状态服务要容器化，得先解决存储与稳定性问题。'),
[qc('云上最常见的形态是？',['只用容器不要虚拟机','虚拟机里跑容器，兼顾隔离与交付效率','只用虚拟机','容器直接跑在裸金属上且不做隔离'],1,'虚拟机提供强隔离边界，容器提供高效交付，两者配合是主流做法。')],
'<p>容器多了以后，靠人手管不过来。→ <a href="#/m/k8s">下一模块：容器编排 K8s</a></p>'
]
];

/* 模块 6:容器编排 K8s(8 课) */
L.k8s = [
['01-why','为什么要编排',6,['编排','声明式','自愈'],
'<p>一句话：容器解决了"怎么打包运行"，但几十上百个容器"谁调度到哪台机器、挂了怎么补、怎么扩容"需要一套系统来管——这就是'+g('k8s','容器编排')+'。</p>',
'<p>手管的痛点很具体：节点挂了上面十几个容器就没了；要扩到 20 个副本得自己写脚本；新版本发布要人工一台台换；容器重启后 IP 变了，调用方找不到它。编排系统把这些变成<b>声明式</b>的：你写下"我要 10 个副本、用这个镜像、暴露这个端口"，系统持续对比现状与期望，差的自己补齐。</p><p>这套"控制器循环"是它的核心思想：不断观察、不断纠偏，所以节点故障、容器崩溃、镜像更新都能自动收敛。理解声明式与命令式的区别，是理解云原生的第一道门。</p>'+ex('半夜某节点故障，上面 8 个容器消失。编排系统在几秒内把它们的副本在其他节点重建，值班人员早上看到的只是告警记录，业务没有中断。'),
pit('别用命令式思维操作声明式系统。手工删一个 Pod 或改一个容器，很快会被控制器"纠正"回来；要改行为就去改期望状态（配置或清单文件），而不是和系统对着干。'),
[qc('Kubernetes 最核心的工作方式是？',['接受命令逐步执行','持续对比期望状态与实际状态并自动纠偏','只在启动时调度一次','人工触发所有操作'],1,'声明式加控制器循环，让系统能自愈与自动达成目标状态。')],
'<p>先看这套系统由哪些部件组成。→ <a href="#/l/k8s/02-architecture">下一课：集群架构</a></p>'
],
['02-architecture','集群架构：控制面与工作节点',7,['controlplane','node','etcd','kubelet'],
'<p>一句话：集群分两部分——'+g('control-plane','控制面')+'是大脑，负责决策与记录；'+g('node','工作节点')+'是手脚，负责真正把容器跑起来。</p>',
'<p>控制面包含几个关键组件：<b>API Server</b> 是所有操作的唯一入口；<b>etcd</b> 是保存集群全部状态的键值数据库（它一旦丢失，集群就失去了记忆）；<b>调度器</b>决定新容器落到哪个节点；<b>控制器管理器</b>负责把各种期望状态落实。工作节点上则跑着 <b>kubelet</b>（向控制面报到、管本机容器）与网络、存储插件。</p><p>这个架构决定了几条运维常识：控制面必须高可用（多副本、etcd 定期备份）；节点可以随时增删，但要保证有足够资源接纳被驱逐的容器；<b>控制面故障不影响已在运行的业务</b>，但所有变更都做不了。</p>'+ex('控制面在做维护时，已运行的业务容器照常工作（kubelet 仍在管着它们），但此时无法创建新容器、无法扩缩容，也无法执行发布——所以这类维护要避开发布窗口。'),
pit('别只备份业务数据，忘了 etcd。etcd 里存着集群的全部配置与状态；没有它，几百个服务的清单、密钥、配置都得重建。规律是：定期快照 + 演练恢复。'),
[qc('etcd 在集群中的作用是？',['存放容器镜像','保存集群全部状态与配置的唯一数据源','代理用户流量','编译代码'],1,'etcd 是集群的记忆，也是备份的重中之重。')],
'<p>接下来看被调度的最小单位。→ <a href="#/l/k8s/03-pod">下一课：Pod</a></p>'
],
['03-pod','Pod：最小调度单位',6,['pod','副本','共享网络'],
'<p>一句话：'+g('pod','Pod')+'是 Kubernetes 里最小的调度单位，可以装一个或多个共享网络与存储的容器，通常一个 Pod 放一个主容器。</p>',
'<p>为什么不直接调度容器？因为有些容器需要"贴在一起"：日志采集边车、代理边车、初始化容器，它们必须共享网络命名空间与卷，能被同时调度、同时迁移。同一个 Pod 内的容器可以用 localhost 互相访问，端口不能冲突。</p><p>Pod 是<b>一次性的</b>：它坏了不会"修复"，而是被删掉重建；重建后 IP 会变、名字会变。所以 Pod 不适合直接暴露给调用方，也不该用来做长久标识——它更像"一批副本中的一个实例"。需要额外能力时，通常加边车容器而不是把多个服务塞进一个容器。</p>'+ex('常见组合：一个主容器跑业务，一个边车容器负责把日志转发到日志系统，还有一个初始化容器在启动前把配置文件准备好。三者同生命周期、同网络视图。'),
pit('别把"多个服务塞进一个 Pod"当省事。它们会被一起调度、一起伸缩、一起重启，任何一个的故障都会拖垮整组，还会让扩缩容粒度失真——拆成独立工作负载才是正解。'),
[qc('同一个 Pod 内的多个容器共享什么？',['文件系统全部内容与进程空间','网络命名空间与部分存储卷','CPU 配额与镜像','没有任何共享'],1,'共享网络与卷是关键：可以用 localhost 互通，但文件系统与进程空间仍相互独立。')],
'<p>Pod 自己不会凭空出现，得有人声明它。→ <a href="#/l/k8s/04-deployment">下一课：Deployment</a></p>'
],
['04-deployment','Deployment：滚动更新与回滚',7,['deployment','滚动更新','回滚','副本'],
'<p>一句话：'+g('deployment','Deployment')+'声明"我要几个副本、用哪个镜像版本"，由它负责滚动更新、版本回滚与副本维持，你不用直接管 Pod。</p>',
'<p>它的工作方式很优雅：改镜像版本时，它不会一次性杀掉所有 Pod，而是按策略逐步替换——先起新版本副本、就绪后摘掉旧副本，直到全部替换完成。整个过程受"最大不可用""最大超量"两个参数控制：想快就允许多起一些，想稳就保证始终有足够可用副本。出问题时一条回滚命令就能退回上一版本。</p><p>版本历史要留着，回滚才有依据；同时要配置<b>就绪探针</b>，否则新版本还没真正能服务就被接入流量，滚动更新就变成了滚动故障。</p>'+ex('一次线上发布：把镜像从 v1.2 改到 v1.3，系统逐个替换 6 个副本；第二个新副本启动后没通过就绪检查，发布自动停住，线上仍有一半旧版本在服务——发现问题及时回滚，用户几乎无感。'),
pit('别把滚动更新的成败交给运气。没有就绪探针、没有资源请求与限制、没有优雅停机的应用，滚动更新会出现"新旧版本同时接流量""旧 Pod 被强杀导致请求失败"等问题。发布前把这几项配齐。'),
[qc('Deployment 滚动更新失败时最可靠的恢复手段是？',['重启集群','回滚到上一个已知可用版本','手工删 Pod','把副本调到 0'],1,'保留版本历史并能一键回滚，是发布安全的基本保障。')],
'<p>副本换来换去，调用方怎么找到它们？→ <a href="#/l/k8s/05-service">下一课：Service 与 Ingress</a></p>'
],
['05-service','Service 与 Ingress：稳定的入口',7,['service','ingress','负载均衡','固定入口'],
'<p>一句话：'+g('service','Service')+'给一组随时增减的 Pod 提供固定访问地址与负载均衡；'+g('ingress','Ingress')+'在集群边界按域名与路径把外部请求转发进来。</p>',
'<p>Pod 的 IP 会随重建而变，直接调用它等于把地址写死在代码里。Service 用一个稳定的虚拟地址加一组标签选择器，把流量分给当前所有匹配的 Pod；后端实例变化时，转发目标自动跟着更新。集群内部调用服务用这个名字就够了。</p><p>外部访问则分两层：四层用负载均衡类型的 Service 直接暴露，或者用 Ingress 做七层入口——按域名和路径路由（api.example.com 走订单服务、/static 走静态资源）、集中配置证书与限流。它是集群的"前台"，好处是所有对外规则集中在一处管理，而不是散落在每个服务里。</p>'+ex('典型做法：内部服务之间只用集群内地址互调，不暴露到集群外；只有 Ingress 对公网开放，统一挂证书、统一做限流。攻击面小、规则集中。'),
pit('别把内部服务用公网可达的方式暴露出去。很多事故的起点就是"为了方便调试"给某个服务开了公网负载均衡，结果没有任何鉴权地被扫描到。内外网入口要严格分开。'),
[qc('为什么调用方不应该直接指向 Pod 的 IP？',['Pod IP 不合法','Pod 是临时的，重建后 IP 会变','Pod 不能联网','这样不安全'],1,'Pod 随时被替换，Service 提供的稳定入口才是正确的调用方式。')],
'<p>配置和密码塞在镜像里不好，那放哪？→ <a href="#/l/k8s/06-config-secret">下一课：配置与密钥管理</a></p>'
],
['06-config-secret','配置与密钥怎么管',6,['configmap','secret','配置分离'],
'<p>一句话：'+g('configmap-secret','ConfigMap 与 Secret')+'把配置和敏感信息从镜像里拆出来，用配置对象注入容器——同一个镜像在不同环境用不同配置，改配置不必重新构建。</p>',
'<p>ConfigMap 放普通配置：连接地址、功能开关、参数阈值；Secret 放敏感信息：数据库密码、令牌、证书。两者都可以按环境变量注入，也可以挂成文件。这类设计解决两个痛点：<b>镜像不需要变</b>（开发、测试、生产用同一个镜像，配置不同）、<b>改配置不必重新打包</b>（改配置对象后滚动重启即可生效）。</p><p>注意 Secret 只是"集中管理"，并不是默认加密存储——很多实现里它只是做了编码，能读该命名空间的人就可能读出来。真正要保护，还得靠<b>访问权限最小化、开启静态加密、必要时接密钥管理服务</b>。</p>'+ex('同一个镜像部署到三套环境：通过不同的 ConfigMap 提供各自的数据源地址，通过 Secret 提供各自的数据库密码，环境差异全部收敛在配置对象里，代码与镜像零改动。'),
pit('别把 Secret 当成保险箱，也别把密钥提交进 Git。集群内的访问控制、静态加密、外部密钥服务都是可选加装项，默认配置往往不够安全；密钥一旦进过代码仓库，就该视为已泄露并轮换。'),
[qc('把配置放进 ConfigMap 的主要好处是？',['镜像更小','同一镜像可用于多环境，改配置无需重新构建镜像','启动更快','更省内存'],1,'配置与镜像解耦，让环境差异不再需要重新打包。')],
'<p>容器删了数据就丢，那持久数据放哪？→ <a href="#/l/k8s/07-pv-pvc">下一课：存储与持久化</a></p>'
],
['07-pv-pvc','存储卷与持久化',6,['pv','pvc','持久化','有状态'],
'<p>一句话：'+g('pv-pvc','PV 与 PVC')+'把存储的供给与申请分开——管理员准备存储（PV），应用按需申领（PVC），Pod 销毁重建后数据还在。</p>',
'<p>为什么要有申请这一层？因为应用开发者不该关心底层是哪种云盘、在哪个可用区，只要说"我要 100G 可读写存储"；存储管理员则负责把它对上。这层抽象还让同一份清单能适配不同环境：云上自动创建云盘，测试环境用本地目录，接口不变。</p><p>访问模式是常被忽略的细节：单节点读写最常用，多节点同时读写需要支持共享的文件存储或对象存储，并不是所有后端都支持。有状态服务（数据库、消息队列）上云原生时，稳定性、数据一致性、备份方案必须一并设计，这也是"有状态服务比无状态难得多"的原因。</p>'+ex('一个数据库容器的清单里申领 200G 存储，Pod 被调度到其他节点重建时，存储被自动挂到新节点上，数据依然在——靠的就是这层解耦的存储抽象。'),
pit('别指望"容器重建数据自动找回"是默认行为。没挂持久卷的话，数据就写在了容器可写层，容器一删全没。有状态服务上云前，先确认：存储后端是什么、能不能跟随调度、备份怎么做。'),
[qc('PVC 的作用是？',['实际存放数据','应用对存储的需求声明，由系统匹配到具体存储','压缩数据','加密数据'],1,'PVC 是"申请"，PV 是"供给"，两者靠绑定完成对接，让应用与底层存储解耦。')],
'<p>最后一课看它怎么决定"跑得好不好、够不够"。→ <a href="#/l/k8s/08-probe-hpa">下一课：探针、调度与自动扩缩</a></p>'
],
['08-probe-hpa','探针、调度与自动扩缩',7,['probe','hpa','requests','调度'],
'<p>一句话：'+g('probe','探针')+'决定系统怎么判断容器"活着、能干活"，'+g('hpa','HPA')+'决定按什么指标自动加副本，两者配错会让自愈和弹性变成事故源。</p>',
'<p>两类探针职责不同：<b>存活探针</b>失败就重启容器，用来处理卡死；<b>就绪探针</b>失败则把实例从流量里摘掉，用来避免把请求发给没准备好的实例。配错的典型症状是"容器反复重启但服务其实是好的"（存活探针太苛刻），或者"发布期间大量 502"（没有就绪探针）。</p><p>调度与扩缩都依赖<b>资源请求与限制</b>：请求值决定调度器认为这个 Pod 需要多少资源，限制值决定它最多能用多少，而 HPA 常用 CPU 使用率（相对请求值算）来加减副本。这也是为什么"不写 requests"会导致调度混乱与弹性失灵。还要注意优雅停机时间要长于业务处理时间，否则扩容还没干活就被杀。</p>'+ex('把就绪探针指向真正的业务健康接口（含依赖检查），把存活探针指向一个轻量端点，再配上合理的 requests 与 HPA 目标值，滚动发布与弹性扩容才都是安全的。'),
pit('别让存活探针依赖外部系统。如果健康检查里包含了"数据库能连上"，那么数据库抖动会导致所有容器被批量重启，把小故障放大成全局雪崩。存活探针要轻、要只看自身。'),
[qc('存活探针与就绪探针的关键区别是？',['没有区别','存活失败重启容器，就绪失败只摘流量','就绪探针更重要','存活探针只在启动时执行'],1,'一个管"要不要重启"，一个管"要不要接流量"，写反了会把小问题放大。')],
'<p>容器都编排好了，服务之间怎么协作？→ <a href="#/m/microservice">下一模块：微服务与治理</a></p>'
]
];
