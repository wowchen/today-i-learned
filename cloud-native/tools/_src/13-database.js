/* 模块 13:数据库与数据工程(7 课) */
L.database = [
['01-why','从文件到数据库',6,['数据库','并发','一致性'],
'<p>一句话：程序开始用文件存数据后，很快就会撞上三堵墙——<b>并发写坏数据、查询只能靠遍历、故障就丢全量</b>，'+g('relational-db','数据库')+'就是为拆掉这三堵墙而生的。</p>',
'<p>用文件存数据的麻烦很具体：两个人同时下单，各自读取库存 10、各减去 1、各写回 9，最后卖出 2 件却只减了 1 件；要按条件统计一个月的订单，只能把文件全读进内存自己筛；机器一断电，写到一半的文件就是垃圾。数据库用三个机制分别应对：<b>事务</b>保证一组操作要么全成要么全不成，<b>索引与查询语言</b>让检索不必遍历全量，<b>日志与副本</b>保证故障后数据仍在。</p><p>所以数据库不是"更高级的文件"，而是一套把并发、查询与可靠性都包好的服务。云上又给了它第二种形态：托管数据库——底层备份、主从、扩容由云负责，你只需要选规格与开权限。</p>'+ex('一个自研系统早期用 JSON 文件存配置，多人同时改出现互相覆盖，最后只保留一个"文件锁 + 重写全量"的土办法。换成数据库后这类问题消失，还顺带拿到了历史查询能力。'),
pit('别把数据库当成"能 SQL 的文件柜"。它的可靠性来自一整套机制（日志、锁、副本），因此也有成本：连接数有限、写入有开销、跨库事务困难。理解这些约束，才能选对数据库类型。'),
[qc('数据库相对文件存数据最核心的三个能力是？',['更省空间、更快、更安全','事务保证、索引与查询、故障恢复','支持更多编程语言、跨平台、开源','可视化界面、报表、导入导出'],1,'并发一致、可检索、故障不丢，正是数据库替代文件存储的根本原因。')],
'<p>先看最主流的那一类数据库怎么组织数据。→ <a href="#/l/database/02-relational">下一课：关系模型与 SQL</a></p>'
],
['02-relational','关系模型与 SQL',6,['关系型','sql','join'],
'<p>一句话：关系型数据库把数据放进一张张"表"里，靠主键关联、用 '+g('relational-db','SQL')+' 表达"要什么"，而不是写"怎么一步步取"。</p>',
'<p>表就是二维结构：订单表一行一条订单，用户表一行一个用户，用户 ID 把两张表关联起来，这就是关系。查询时你写"我要华东区上个月的订单金额合计"，由数据库自己决定怎么用索引、怎么扫描、怎么聚合——这种<b>声明式</b>表达是它最大的便利。</p><p>拿云上项目说，最常见的一组表是：业务主表 + 明细表 + 字典表 + 流水表。设计上的关键动作是<b>定主键、定外键关系、定字段类型与约束</b>；把约束交给数据库（唯一、非空、外键），比只靠应用代码校验可靠得多，因为并发与异常路径下代码常常漏判。</p>'+ex('一条典型的联表统计：从订单表按时间过滤、关联用户表取出所属区域、按区域分组求和。业务代码只需声明条件与聚合方式，执行计划与索引选择交给数据库优化器。'),
pit('别把"能存就能查"当成设计完成。字段类型选错（用字符串存金额、用文本存时间）、缺少唯一约束造成重复数据，是后期最难修的技术债——数据一旦脏了，清理成本远超当初设计的成本。'),
[qc('关系型数据库的查询方式是？',['写清楚遍历步骤','声明要什么，由数据库决定怎么取','必须自己维护索引文件','只能按主键查询'],1,'声明式查询把"怎么取"交给优化器，这是 SQL 的核心便利。')],
'<p>多个人同时改同一行数据，靠什么不乱？→ <a href="#/l/database/03-acid">下一课：事务与 ACID</a></p>'
],
['03-acid','事务、并发与锁',7,['acid','事务','隔离级别'],
'<p>一句话：'+g('acid','事务')+'是一组"要么全成功、要么全回滚"的操作，它靠<b>原子性、一致性、隔离性、持久性</b>四条约定，把并发世界里的数据守住。</p>',
'<p>转账是最经典的例子：A 扣款与 B 入账必须同生共死，中间断电也不能出现"扣了没到账"。原子性保证这一点；持久性保证提交后的数据即使立即断电也在；隔离性则是让并发事务互不干扰——但完全的隔离代价太高，所以数据库提供了不同的<b>隔离级别</b>：级别越低并发能力越强，越可能出现"读到别人未提交的数据"或"同一事务内两次读结果不同"这类现象。</p><p>工程上的常见做法是"<b>读已提交</b>为主 + 关键路径加锁或用乐观锁版本号"。乐观锁的典型实现是给记录加版本字段，更新时带版本比对，版本不一致就重试——它比长事务加锁更轻，特别适合读多写少的互联网业务。</p>'+ex('库存扣减用乐观锁：读取时拿到版本号 5，提交时更新条件写成"版本=5"，若被别的请求先改过则更新行数为 0，应用重试一次。既避免了长锁等待，也杜绝了超卖。'),
pit('别把长事务当习惯。事务里夹着调用外部接口、处理大文件、等待用户输入，会长时间占锁，把并发能力拖垮。正确做法是事务只包住必要的数据库操作，外部动作放到事务之外并用补偿逻辑兜底。'),
[qc('高并发下扣减库存常用的做法是？',['长事务加表锁','读版本号 + 条件更新 + 失败重试','先扣再查','不做任何并发控制'],1,'乐观锁用版本比对替代长锁，兼顾正确性与并发能力。')],
'<p>数据多了以后，为什么查询会变慢？→ <a href="#/l/database/04-index">下一课：索引与慢查询</a></p>'
],
['04-index','索引与慢查询',6,['索引','慢查询','执行计划'],
'<p>一句话：'+g('index','索引')+'像书的目录，让数据库能直接跳到目标位置而不是逐页翻——代价是占空间，并让每次写入都要顺带维护目录。</p>',
'<p>没有索引时，按用户 ID 查订单要从头扫全表。索引把常用检索字段排序成便于二分查找的结构，查询从"全表扫描"变成"定位若干页"。但索引不是越多越好：每个索引都要在增删改时更新，表上十个索引会让写入明显变慢，占用的空间也可能超过数据本身。</p><p>排障的正确顺序是：先看<b>慢查询日志</b>找出耗时最长的语句，再看<b>执行计划</b>判断是走了索引还是扫了全表，最后才决定加索引或改写语句。常见无效情况包括：在索引列上做函数运算、类型不匹配导致隐式转换、以及<b>索引失效的最阴险形式——字段区分度太低</b>（比如只按"性别"建索引，筛选后仍剩一半数据）。</p>'+ex('一张千万级订单表原本按用户 ID 查询要 3 秒。分析执行计划发现是全表扫描，补上"用户 ID + 创建时间"的联合索引后降到十几毫秒——同一条 SQL，索引让结果从"能不能用"变成"好不好用"。'),
pit('别在写查询时忽略数据量假设。小表上怎么写都快，一旦数据涨到千万级，当初随手写的模糊匹配、函数筛选、多表嵌套就会集中爆发。上线前用真实量级压一次，比事后加索引便宜得多。'),
[qc('索引带来的主要代价是？',['查询变慢','占空间并拖慢写入，因为每次写都要维护索引','不支持事务','不能用于排序'],1,'索引是典型的"读换写"，所以要按查询模式精挑，而不是见字段就建。')],
'<p>关系型并不是唯一答案。→ <a href="#/l/database/05-nosql">下一课：关系型之外的选择</a></p>'
],
['05-nosql','关系型之外的选择',6,['nosql','键值','文档','宽表'],
'<p>一句话：'+g('nosql','NoSQL')+'是为绕开关系型的某些限制而生的家族——键值、文档、宽表、图，各自擅长不同形态的数据与规模。</p>',
'<p>四类最常遇到：<b>键值</b>（如缓存与配置，按 key 极速读写）、<b>文档</b>（存结构灵活的 JSON，字段随时增删，适合快速迭代的业务对象）、<b>宽表</b>（海量写入、按分区键高效查询，适合日志与物联网时序数据）、<b>图</b>（存实体与关系，适合关系推理与链路分析）。它们的共同取向是<b>牺牲一部分强一致与联表能力，换取水平扩展与灵活结构</b>。</p><p>选型的关键不是"哪种更先进"，而是<b>访问模式</b>：先问"我的查询长什么样、数据量会长到多大、能不能接受最终一致"。绝大多数业务系统的主库仍是关系型——因为交易要事务、报表要联表；而缓存、日志、检索、推荐特征这些环节交给 NoSQL 更合适。</p>'+ex('一个典型组合：订单主库用关系型（要事务），商品详情缓存用键值（要快），商品搜索用检索型（要全文与多维筛选），行为日志用宽表（要海量写入）。数据库选型其实是"按访问模式分工"。'),
pit('别因为"要扩展"就换掉关系型。多数系统的瓶颈在慢查询、缺索引或缺缓存，而不是数据库类型；盲目换成 NoSQL 会同时失去事务、联表与成熟的运维经验，代价远大于收益。'),
[qc('选择数据库类型时最重要的依据是？',['社区热度','真实访问模式：查询形态、数据规模、一致性要求','License 是否免费','是否出自大厂'],1,'按访问模式分工，而不是按流行度选型，才能避免迁移成本。')],
'<p>单库性能到顶了怎么办？→ <a href="#/l/database/06-scale">下一课：复制、读写分离与分库分表</a></p>'
],
['06-scale','复制、读写分离与分库分表',7,['replication','sharding','读写分离'],
'<p>一句话：扩展数据库有三步——先加'+g('cache-db','缓存')+'、再做'+g('replication','读写分离')+'、最后才'+g('sharding','分库分表')+'，顺序颠倒会把复杂度提前引入。</p>',
'<p>第一步最划算：把热点数据放进内存缓存，数据库压力常能降一个量级，代价是缓存与数据库之间的一致性问题（常用过期时间 + 主动失效控制）。第二步做读写分离：主库负责写入、多个从库承担读请求，同时从库天然成为故障切换的备选；代价是复制延迟，可能出现"刚写完查不到"，所以<b>写完立刻要读的关键路径必须走主库</b>。</p><p>第三步最重：单库数据量或写入量确实到顶时，按规则（如用户 ID 取模、按时间分区）把数据拆到多个库表。分片带来的新问题是<b>跨片查询、跨片事务、扩容时重新分布数据</b>，工程复杂度陡增。所以正确顺序是"索引与慢查询优化 → 缓存 → 读写分离 → 垂直拆分 → 水平分片"。</p>'+fml('扩展顺序：慢查询优化 → 缓存 → 读写分离 → 分片（复杂度逐级上升，收益未必）')+ex('一次典型的演进：某系统 QPS 到 3000 时数据库告警，先加缓存把读请求挡掉七成，再做读写分离分散剩余压力，最终推迟了两年才需要分片。'),
pit('别一上来就分库分表。它会把联表查询、事务、统计报表、运维备份全部变成难题；很多"必须分片"的判断，其实是因为前面几步（索引、缓存、慢 SQL、大字段拆分）没做。'),
[qc('数据库扩展的推荐顺序是？',['先分库分表再优化','索引与慢查询优化 → 缓存 → 读写分离 → 分片','直接上分布式数据库','扩大单机磁盘'],1,'复杂度逐级上升，把简单手段用尽再引入分片，才是成本最优路径。')],
'<p>业务库之外，还有一整套面向分析的数据工程。→ <a href="#/l/database/07-data-engineering">下一课：从业务库到数仓</a></p>'
],
['07-data-engineering','从业务库到数仓',7,['数据仓库','etl','olap','数据质量'],
'<p>一句话：业务库负责"把每笔交易记准"，'+g('data-warehouse','数据仓库')+'负责"把海量历史算明白"——两者目标相反，所以必须分开，中间靠数据工程把它们连起来。</p>',
'<p>区别在负载性质：业务库是 '+g('oltp-olap','OLTP')+'（高频小事务、要快、要准），数仓是 OLAP（大批量扫描聚合、容忍分钟级延迟）。如果在业务库上直接跑报表，一个全量统计就能把线上拖慢，所以数据要经由 '+g('etl','ETL/ELT')+' 抽取转换加载同步到分析层，并按分层建模（贴源层 → 明细层 → 汇总层 → 应用层），越往上越贴近指标口径。</p><p>近年的主流形态是'+g('lakehouse','湖仓')+'：底层用对象存储放原始数据（便宜、容量近乎无限），上面加表格式与事务能力，让数据湖也能像数仓那样做增量更新与查询。无论形态如何，决定报表可信度的往往不是引擎，而是'+g('data-quality','数据质量')+'——口径是否统一、缺失与重复是否治理、时间字段是否对齐。</p>'+ex('一个常见的分歧场景：两个部门报同一指标数字不一致，追查发现一个按"支付时间"、一个按"下单时间"，还有一个把退款算成了负收入。解决方式不是改代码，而是先定指标口径并写进数据字典。'),
pit('别把"数据量大"直接等同于"要上大数据平台"。很多分析需求在千万级数据上用成熟的数仓加上合理索引就能满足；先明确指标与口径，再谈技术栈，否则容易建一套昂贵而没人用的平台。'),
[qc('业务库与数仓必须分开的根本原因是？',['成本不同','交易型与分析型负载特性相反，混在一起会互相拖垮','厂商要求','数据量太大'],1,'OLTP 追求高频小事务的稳定延迟，OLAP 追求大批量扫描，两者互相干扰。')],
'<p>数据存好了，接下来看它在云上怎么被访问。→ <a href="#/m/cloudnet">下一模块：云网络</a></p>'
]
];
