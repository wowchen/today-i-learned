/* 模块 3:存储与备份(7 课) */
L.storage = [
['01-three-kinds','块、文件、对象三兄弟',6,['块存储','文件存储','对象存储'],
'<p>一句话：云存储就三类——'+g('block-storage','块存储')+'像裸硬盘、'+g('file-storage','文件存储')+'像共享文件夹、'+g('object-storage','对象存储')+'像无限大的寄存柜，选错类型比买贵更麻烦。</p>',
'<p><b>块存储</b>是裸设备，挂给主机后要自己分区格式化，性能最好、延迟最低，数据库跑在它上面。<b>文件存储</b>有目录树、能被多台机器同时挂载，适合共享文档、容器共享配置、老系统的共享盘。<b>对象存储</b>用 HTTP 接口按"桶 + 键"存取，没有真正的目录层级，单个文件可以很大、总量几乎无限，适合图片、视频、备份包、日志归档。</p><p>判断口诀很实用：<b>要装数据库、要极致 IO，用块；要多人多机共享一个目录，用文件；要海量存文件、通过链接访问，用对象。</b></p>'+ex('一个视频类应用的典型分工：数据库放块存储，转码程序的工作目录用文件存储共享，转好的视频文件存进对象存储，前端通过 CDN 从对象存储拉取。'),
pit('别把对象存储当文件系统用。它不支持"打开一个文件改中间几个字节"，也不擅长频繁重命名与目录遍历；把需要随机读写的数据硬塞进对象存储，性能与成本都会很难看。'),
[qc('需要跑数据库、追求低延迟随机读写，应该选？',['对象存储','块存储','文件存储','随便哪种都行'],1,'块存储提供裸设备的随机 IO 能力，延迟最低，是数据库的首选。')],
'<p>先看性能最好的那块"裸硬盘"。→ <a href="#/l/storage/02-block">下一课：块存储</a></p>'
],
['02-block','块存储：给主机挂块裸硬盘',5,['块存储','云硬盘','iops'],
'<p>一句话：块存储就是云上的一块硬盘，挂给一台主机使用，性能以 IOPS 和吞吐衡量，数据库与高负载应用靠它。</p>',
'<p>云硬盘按介质分档：普通云盘便宜但 IOPS 低，SSD 云盘性能好，增强型 SSD 或 ESSD 类可以到几万甚至几十万 IOPS。容量与性能在多数云上<b>绑定</b>——盘越大，可用 IOPS 与带宽上限越高，所以"小容量要高性能"往往做不到，只能通过挂多块盘做条带化来突破。</p><p>块存储通常只能挂给<b>同一可用区内</b>的一台实例（部分支持多重挂载），因为它本质是给单机用的硬盘。系统盘与数据盘要分开：系统盘随实例生命周期，数据盘独立存在，实例释放时数据盘可以保留，这是防止误删的关键习惯。</p>'+ex('数据库看到磁盘 IO 打满、延迟飙高：先看盘的类型与容量档位，很多"数据库慢"其实是云盘 IOPS 上限到了，换更高档位或加盘条带立刻缓解。'),
pit('别把重要数据只放系统盘。系统盘随实例销毁而消失，误删实例会连数据一起带走。生产上数据必须放独立数据盘，并且开启自动快照策略。'),
[qc('云硬盘的 IOPS 上限通常与什么相关？',['实例的操作系统','盘的类型与容量档位','用户名','所在子网'],1,'多数云上容量与性能绑定，小盘拿不到高 IOPS，这是常见性能陷阱。')],
'<p>如果多台机器要共享一个目录呢？→ <a href="#/l/storage/03-file">下一课：文件存储</a></p>'
],
['03-file','文件存储：多台机器共享目录',5,['文件存储','nfs','共享'],
'<p>一句话：文件存储提供带目录树的共享文件系统，多台机器可以同时挂载读写，就像大家共用一个大网盘。</p>',
'<p>它最典型的协议是 NFS（Linux 世界）与 SMB（Windows 世界）。适用场景很明确：多台 Web 服务器共享上传目录、容器集群共享配置文件与静态资源、老系统迁移上云时保留原有文件访问方式、数据分析多节点读取同一批数据。</p><p>它的性能特点与块存储不同：<b>共享</b>带来并发读写，但也带来一致性开销，随机小文件性能通常不如本地盘。选型上按吞吐与并发量挑档位，并注意配额——一个应用写满共享目录会把所有挂载它的机器一起拖垮。</p>'+ex('三台应用服务器要从同一个目录读取用户上传的附件：用文件存储挂载到同样路径，扩容到十台也不用改代码；如果用块存储，每台机器各有各的盘，文件根本对不上。'),
pit('别用文件存储跑数据库，也别在高并发下大量写小文件。共享文件系统的锁与元数据开销会放大延迟，遇到"多机同时写同一个文件"更是容易冲突——这类需求应该改成对象存储加应用层协调。'),
[qc('文件存储最适合的场景是？',['数据库主库','多台机器共享同一份文件','超高 IOPS 的缓存','离线归档'],1,'共享目录树、多机同时访问，正是文件存储的定位。')],
'<p>海量文件怎么办？→ <a href="#/l/storage/04-object">下一课：对象存储</a></p>'
],
['04-object','对象存储：无限大的寄存柜',6,['对象存储','bucket','http'],
'<p>一句话：'+g('object-storage','对象存储')+'用 HTTP 接口按"'+g('bucket','存储桶')+' + 对象键"存取文件，容量近乎无限、单价便宜、天然适合互联网访问。</p>',
'<p>它把数据当成"对象"：每个对象有内容、有元数据、有唯一键（形如 bucket/photos/2026/a.jpg）。没有真正的目录，路径只是键名的前缀，所以遍历与重命名都很低效——它的强项是<b>一次写入、多次读取</b>：图片、音视频、备份包、日志归档、静态网站资源。</p><p>三个必知特性：<b>存储分级</b>（标准、低频、归档，越冷越便宜）、<b>生命周期规则</b>（到期自动转冷或删除）、<b>访问控制</b>（公开读、私有、签名 URL 临时授权）。对象存储还是大数据与 AI 场景的事实标准底座——很多计算引擎直接读对象存储，省掉了数据搬运。</p>'+ex('常见的静态站点方案：把网页与图片放进对象存储，开启静态网站托管并挂 CDN，不用买一台服务器就能对外提供服务，费用按存储量与流量算。'),
pit('别把存储桶设成"公开可读"图省事。大量数据泄露事故的起因就是桶权限配置错误——凡是含用户数据、备份、日志的桶，默认必须私有，对外访问一律用签名 URL 或有期限的授权。'),
[qc('对象存储最擅长的访问模式是？',['频繁随机改写文件中间部分','一次写入、多次读取的海量文件','小文件高频重命名','数据库事务'],1,'对象存储为海量读优化，写多改少的场景最划算。')],
'<p>存进来之后，怎么放才省钱？→ <a href="#/l/storage/05-tiering">下一课：存储分级</a></p>'
],
['05-tiering','存储分级：冷热分开放',5,['存储分级','生命周期','归档'],
'<p>一句话：'+g('storage-class','存储分级')+'按数据被访问的频率分档收费——热数据放快而贵的介质，冷数据放慢而便宜的介质，归档数据最便宜但取回要等。</p>',
'<p>数据是有"温度"的：最近一周被频繁读取的热数据可能只占很小比例，却贡献了绝大多数访问；三个月没碰过的冷数据占了大部分容量，却几乎没有访问。把两者放同一档，等于长期为不访问的数据付高价。</p><p>做法两条：<b>分级选择</b>——建桶时就按用途选档位；<b>生命周期规则</b>——设置"上传 30 天后转低频、180 天后转归档、一年后删除"。归档档的存储费可能只有标准档的十分之一，但取回需要解冻，延迟从分钟到小时不等。</p>'+ex('日志类数据的经典策略：近 7 天放标准档供排障查询，8 到 90 天转低频，90 天以上转归档只做合规留存，超过一年自动删除。整套规则配上后，存储费用通常能降一半以上。'),
pit('别把"能不能取回"和"多久能取回"搞混。归档档取回要预约与等待，如果业务上必须秒级读，放归档就是给自己挖坑。分级前先问清楚：这份数据最坏情况下多久内必须拿到。'),
[qc('把三个月未访问的数据转到归档档的收益是？',['读取更快','存储单价显著降低，代价是取回变慢','更安全','可以省掉备份'],1,'分级就是用取回速度换存储成本，适合明确不再频繁访问的数据。')],
'<p>数据存下来了，怎么保证不丢、能回退？→ <a href="#/l/storage/06-snapshot-backup">下一课：快照、备份与容灾</a></p>'
],
['06-snapshot-backup','快照、备份与容灾',6,['快照','备份','容灾','rto'],
'<p>一句话：快照解决"改坏了快速回退"，备份解决"数据丢了能找回"，容灾解决"机房没了业务还能跑"——三层目标不同，缺一层都不安全。</p>',
'<p>用两个指标描述需求：<b>RTO</b>（能容忍停多久）与<b>RPO</b>（能容忍丢多少数据）。要求 RTO 一小时、RPO 五分钟，就得有热备与准实时复制；只要求 RPO 一天，每天一次备份就够了。成本几乎随这两个指标收紧而指数上升，所以先定指标再选方案，别反过来。</p><p>实践上的三条底线：<b>3-2-1 原则</b>（三份数据、两种介质、一份异地）、<b>定期恢复演练</b>（备份必须验证能恢复，没验过的备份不算数）、<b>异地留存</b>（同城备份挡不住区域性灾害）。云上还要注意：快照与源盘常在同一存储体系内，属于"方便"而非"安全"。</p>'+ex('一次真实教训：备份任务每天成功执行、日志全绿，但从未做过恢复演练；真出事时才发现备份文件损坏且缺了关键表。此后团队把"每季度真恢复一次"写进了运维流程。'),
pit('别把 RTO 与 RPO 混淆。RTO 关注"多久能恢复运行"，RPO 关注"会丢多少数据"。讨论容灾时先把这两个数字定下来，否则方案永远在"太贵"和"不够用"之间来回吵。'),
[qc('关于备份最容易被忽略的一条是？',['备份越频繁越好','必须真的演练恢复，验证备份可用','只备数据库就够','存在同一机房就行'],1,'未经验证的备份等于没有备份——恢复演练是容灾体系里最便宜也最常被跳过的一环。')],
'<p>最后看一个抽象但致命的问题：一致性。→ <a href="#/l/storage/07-consistency">下一课：数据一致性</a></p>'
],
['07-consistency','数据一致性：别丢也别乱',6,['一致性','强一致','最终一致','副本'],
'<p>一句话：数据在多个副本之间同步需要时间，<b>强一致</b>保证读到的永远是最新值但延迟高，<b>最终一致</b>性能好但可能短暂读到旧值。</p>',
'<p>为什么会有这个取舍？写一份数据要同步到三个副本，如果要求"写成功即所有副本都更新完"，那任何一次网络抖动都会拖慢甚至失败；如果要求"先返回成功、副本慢慢同步"，性能上去了，但并发读可能拿到旧值。云上的数据库、对象存储、缓存都在这个谱系上选择自己的位置。</p><p>工程上的应对不是争论哪个更好，而是<b>按业务分级</b>：账户余额、库存扣减必须强一致；点赞数、浏览量、推荐列表用最终一致完全够；跨服务的长流程用"最终一致 + 对账补偿"。</p>'+ex('转账场景：主库强一致扣款，成功后异步更新查询库与报表库；用户查余额如果读到旧值会引发投诉，所以余额查询强制走主库，而历史账单允许走从库。'),
pit('别在分布式系统里假设"写完立刻处处可见"。跨可用区、跨服务的读写之间天然有延迟窗口，凡是需要立刻读到最新值的逻辑，都要明确指定读主库或加版本号校验。'),
[qc('账户余额扣减这类操作应该选择？',['最终一致','强一致','不需要一致性','只写缓存'],1,'涉及资金与库存的关键路径必须强一致，性能上的代价是值得的。')],
'<p>数据放好了，接下来看它怎么被访问。→ <a href="#/m/cloudnet">下一模块：云网络</a></p>'
]
];

/* 模块 4:云网络(7 课) */
L.cloudnet = [
['01-vpc','专有网络：云上你自己的地盘',6,['vpc','私网','网段'],
'<p>一句话：'+g('vpc','专有网络')+'是云上划给你独占的一块虚拟网络，IP 段、子网、路由都由你定，别人看不见也进不来。</p>',
'<p>没有 VPC 之前，云上资源像是住在公共宿舍；有了 VPC，等于你有一块自己的院子，围墙内怎么划分都行。创建 VPC 时最重要的是<b>规划网段</b>：通常从 RFC1918 的私有地址里取一段，比如 10.0.0.0/16，再用 /24 的子网切成不同用途的区域。</p><p>网段规划要一次想清楚，因为后期改极麻烦：本地机房要互联的，两边的网段<b>不能重叠</b>；未来要多地域互通的，网段最好有层次（如每个地域一段），否则跨域打通时路由会撞车。很多项目的技术债，就是当年随手用了 192.168.0.0/16 埋下的。</p>'+ex('一种清爽的规划：10.10.0.0/16 给生产、10.20.0.0/16 给测试、10.30.0.0/16 给开发；每个 VPC 内部再按 10.x.1.0/24 划应用子网、10.x.2.0/24 划数据库子网，看 IP 就知道属于哪个环境。'),
pit('别让两个 VPC 或 VPC 与本地机房用同一个网段。一旦重叠，互联时路由无法区分"这个地址到底在哪边"，只能重规划，代价往往是重建整个网络。开工前画一张地址分配表，收益极大。'),
[qc('VPC 规划中最容易留下后患的一点是？',['名字取得不好','与本地机房或其他 VPC 网段重叠','子网太多','没用 IPv6'],1,'网段重叠是网络互联的硬伤，后期只能靠重建或复杂转换来解。')],
'<p>院子有了，还要划楼栋。→ <a href="#/l/cloudnet/02-subnet">下一课：子网与路由表</a></p>'
],
['02-subnet','子网与路由表',6,['子网','路由表','公网私网'],
'<p>一句话：'+g('subnet','子网')+'是 VPC 里按 IP 段切出的区域，用路由表决定流量往哪走——哪些能通公网、哪些只能内网互通，全由它说了算。</p>',
'<p>典型划分是按"能否直接连公网"分层：<b>公网子网</b>放需要被外部访问的负载均衡、跳板机、NAT 网关；<b>私网子网</b>放应用与数据库，默认没有公网出口，需要访问外网时通过 NAT 网关出去。这样外部流量进不来，内部机器还能更新软件包、调用外部接口。</p><p>路由表就是一张"目的地 → 下一跳"的清单：本 VPC 内的流量走本地，出去到别的 VPC 走对等连接或专线，去公网走网关。可用的可用区也要考虑：一个子网只属于一个可用区，要同城双活就得在两个可用区各建一套对应子网，应用部署时按子网落位。</p>'+ex('标准三层结构：前端子网（公网，放负载均衡）、应用子网（私网，放应用实例）、数据子网（私网且不配任何出网路由，放数据库）。三层之间用安全组与路由双重约束。'),
pit('别把数据库放在公网子网里图方便，哪怕只开放一个端口。公网可达的数据库是全网的扫描目标，正确做法是私网部署、通过跳板机或专线访问。'),
[qc('应用服务器既要能被内网访问，又要能出去下载依赖，正确做法是？',['直接放公网子网挂弹性 IP','放私网子网，通过 NAT 网关出网','开一条 0.0.0.0 全通路由','放在安全组外'],1,'私网部署加 NAT 出网，既满足出访需求又不暴露入站入口。')],
'<p>网络通了，还要控制谁能进。→ <a href="#/l/cloudnet/03-security-group">下一课：安全组</a></p>'
],
['03-security-group','安全组：实例的门禁',6,['安全组','防火墙','端口'],
'<p>一句话：'+g('security-group','安全组')+'是挂在实例或网卡上的虚拟防火墙，用一组规则决定哪些来源能访问哪些端口。</p>',
'<p>它与传统防火墙思路一致，但更贴近实例：规则按"方向（入站/出站）+ 协议 + 端口 + 来源"描述，默认拒绝所有入站、允许所有出站是最常见的基线。安全组通常<b>有状态</b>——出去的连接，回来的响应自动放行，不用单独写反向规则。</p><p>实践要点：按<b>用途分组</b>而不是按机器（如"Web 组开 443、应用组只允许 Web 组来访问、数据库组只允许应用组访问"），同一组挂多台机器，改一处全生效；来源尽量写<b>安全组 ID 或网段</b>，不要图省事写 0.0.0.0/0。</p>'+ex('一台机器上多个网卡可以挂不同安全组，也可多组叠加取并集。有人误以为"叠加是取交集"导致排查半天——记住：安全组规则是<b>并集放行</b>，这也是为什么多加一个组反而可能放开权限。'),
pit('别用"先全放开跑通再收紧"当长期方案。临时放开的 0.0.0.0/0:22 常常一直留到下次安全审计。真要临时放行，记录清楚并在当天收回。'),
[qc('实例挂了两个安全组，放行结果是？',['取交集，更难通过','取并集，更容易通过','以后挂的为准','随机生效'],1,'安全组规则是并集——这也是"多加组反而更宽松"的原因，配置时要留意。')],
'<p>多台机器前面通常还要一个分流器。→ <a href="#/l/cloudnet/04-slb">下一课：负载均衡</a></p>'
],
['04-slb','负载均衡：不把压力压在一台上',6,['负载均衡','健康检查','四层七层'],
'<p>一句话：'+g('slb','负载均衡')+'把进来的流量按规则分给多台后端，并持续做健康检查，坏掉的机器自动踢出。</p>',
'<p>它解决两个问题：<b>分流</b>（一台扛不住就分给多台，横向扩展成为可能）与<b>高可用</b>（后端某台挂了，健康检查发现后不再向它转发，用户几乎无感）。按工作层次分：四层（TCP/UDP）只按连接转发，快、通用；七层（HTTP/HTTPS）能看域名与路径，可以做路由、会话保持、证书卸载、灰度分流。</p><p>负载均衡自身也必须是高可用的——云上的托管负载均衡通常跨可用区部署，实例规格则决定并发连接数与带宽上限，这是"突然大量 502"的常见根因之一。</p>'+ex('一个典型配置：同一套后端同时挂两台应用服务器，开启健康检查。滚动发布新版本时，先把一半后端从负载均衡摘掉、升级、验证、再挂回，用户全程无感。'),
pit('别只看后端有没有掉线，忘了看负载均衡自身的指标。连接数或带宽打满时，表现是间歇性 502 或超时，而后端监控一切正常——排查时先看入口这一层。'),
[qc('七层负载均衡相比四层多出来的能力是？',['加密更快','按域名与路径做应用层路由','不需要后端','可以省带宽'],1,'七层能看懂 HTTP 内容，所以能做路径路由、会话保持、证书卸载。')],
'<p>内网机器怎么出去、外网地址怎么变？→ <a href="#/l/cloudnet/05-nat-eip">下一课：NAT 网关与弹性公网 IP</a></p>'
],
['05-nat-eip','NAT 网关与弹性公网 IP',6,['nat网关','eip','出网'],
'<p>一句话：'+g('nat-gateway','NAT 网关')+'让私网机器能出去访问外网、但外网进不来；'+g('eip','弹性公网 IP')+'是可以随时绑定与解绑的公网地址。</p>',
'<p>私网实例没有公网地址，需要装依赖、调用外部接口时怎么办？让流量走 NAT 网关，网关用它自己的公网地址做地址转换，把请求发出去、把回应带回来。这样内部机器从不暴露在公网，出访需求也满足了，是生产环境的标配。</p><p>弹性公网 IP 则是"可以带着走"的地址：从一台实例解绑、绑到另一台，外部看到的地址不变，适合需要固定 IP 的对端配置（如白名单、专线互联）。但它和实例是一对多绑定的，想把一个地址分给多台机器，那应该是负载均衡的活。</p>'+ex('对端系统要求固定出口 IP：给 NAT 网关绑定若干弹性公网 IP，对方只需把这几 个地址加进白名单，后端机器怎么扩缩、换 IP 都不影响对接。'),
pit('别把弹性公网 IP 当成"随便挂"的资源。它通常按占用时长收费，即使没绑到实例上也在计费；而且直接给业务实例绑公网 IP 会暴露入口，能用负载均衡与 NAT 就别直接用。'),
[qc('私网实例要访问外部接口但不暴露自己，应该？',['直接绑弹性公网 IP','通过 NAT 网关出网','改安全组放行入站','用对象存储代理'],1,'NAT 网关提供只出的通道，内部实例的入站面保持关闭。')],
'<p>如果云上要和本地机房打通呢？→ <a href="#/l/cloudnet/06-direct-connect">下一课：云专线</a></p>'
],
['06-direct-connect','云专线与跨域互通',6,['云专线','对等连接','跨域'],
'<p>一句话：'+g('direct-connect','云专线')+'是从你的机房或办公楼拉一条专用链路直连云上 VPC，不走公网、延迟稳定、带宽可保证。</p>',
'<p>走公网访问云上数据库，不仅慢，更重要的是不安全、抖动大。专线把两端用物理或运营商的专用通道连起来，全程不经过互联网，延迟可预测，适合"本地系统调云上服务""本地数据同步上云"这类场景。它的建设周期较长、成本较高，所以常见的过渡方案是 IPsec VPN：配置快、能加密，但带宽与稳定性不如专线。</p><p>云内部跨 VPC 的互通则通过<b>对等连接</b>或云企业网类的产品实现，把多个 VPC、多个地域、多个账号的网络连成一张图。这时前面强调的"网段不能重叠"就变成硬约束——一旦重叠，连不上。</p>'+ex('混合云最常见的一条链路：本地机房与云上 VPC 用专线互联，本地应用通过内网地址直接访问云上数据库；公网侧只保留必要的对外服务入口，其余全部关闭。'),
pit('别以为专线等于高可用。专线也可能被施工挖断，关键链路要配两条不同路径的专线或"专线 + VPN"互备；同时记住云上安全组与路由仍要按最小范围配置，专线不是免检通道。'),
[qc('专线相比公网访问云上资源的主要优势是？',['完全免费','不走公网、延迟稳定且带宽可保证','配置更简单','不需要安全组'],1,'专用通道带来可预测的延迟与带宽，这对数据库等敏感流量很关键。')],
'<p>最后一课看面向公众的加速与解析。→ <a href="#/l/cloudnet/07-cdn-dns">下一课：CDN 与云 DNS</a></p>'
],
['07-cdn-dns','CDN 与云 DNS 加速',6,['cdn','dns','缓存'],
'<p>一句话：'+g('cdn','CDN')+'把静态内容提前缓存到离用户最近的节点，'+g('cdn','云 DNS')+'把域名解析到最合适的入口——一个管内容近、一个管路由对。</p>',
'<p>用户在北京访问广州机房的图片，物理距离带来几十毫秒延迟，加上带宽成本，体验与费用都不佳。CDN 的办法是把图片复制到各地边缘节点，用户就近取，回源压力也大幅下降。<b>缓存策略</b>是它的核心：静态资源设长缓存并靠文件名带版本号刷新，动态接口不缓存或极短缓存。</p><p>云 DNS 负责把域名解析到正确的入口：支持按地域解析（不同地区返回不同机房）、按权重解析（灰度切换）、故障时自动切换。它和负载均衡、CDN 一起构成了"用户 → 最近入口 → 多后台"的完整链路。</p>'+ex('典型的静态加速配置：图片与 JS/CSS 走 CDN 长缓存，文件名带内容摘要；接口请求走负载均衡到应用；域名按地域解析到就近入口。用户感知是"网站变快了"，后台是三层各司其职。'),
pit('别给带用户信息的动态接口开 CDN 缓存。一旦被缓存，A 用户可能看到 B 用户的数据——这是非常严重的隐私事故。动态内容要明确设置不缓存，或按用户维度隔离键值。'),
[qc('CDN 缓存最适合的内容是？',['带用户令牌的接口响应','图片、样式、脚本等静态资源','数据库查询结果','实时行情'],1,'静态资源不随用户变化、可长缓存，是 CDN 的主战场。')],
'<p>网络打通了，接下来看应用怎么被打包和运行。→ <a href="#/m/container">下一模块：容器技术</a></p>'
]
];
