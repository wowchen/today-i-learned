/* 模块 7:微服务与治理(7 课) */
L.microservice = [
['01-monolith','从单体说起',6,['单体','微服务','拆分动因'],
'<p>一句话：'+g('monolith','单体应用')+'把所有功能打成一个程序一起构建一起部署，'+g('microservice','微服务')+'把它拆成多个能独立运行的小服务——拆分是为了解决协作与扩展问题，不是为了时髦。</p>',
'<p>单体不是原罪。业务早期单体效率极高：一个仓库、一次构建、一次部署，改一个功能不用考虑接口兼容；出错时日志都在一处，排查容易。问题出在规模上来之后：代码库几十万行，几十个人在一个仓库里改，一次上线要等所有组都准备好；某个不重要的模块要扩容，只能把整个应用复制一份，浪费资源；一处内存泄漏拖垮全站。</p><p>微服务的核心收益是<b>解耦</b>：每个服务独立开发、独立部署、独立扩容、独立选技术栈。代价同样真实：网络调用替代了进程内调用，于是延迟、失败、数据一致性、可观测性全都变成要专门解决的问题。所以拆分的时机应该是"痛到必须拆"，而不是"一开始就拆"。</p>'+ex('一个常见的演进路径：先做单体模块化（代码分模块、边界清晰），痛点出现后再把变化最频繁或负载最特殊的模块先拆出去，而不是一次性全域拆成几十个服务。'),
pit('别把微服务当成技术水平的分水岭。拆早了会带来分布式系统的全部复杂度却拿不到收益；判断标准是协作冲突、独立扩容需求、发布节奏差异这三条，出现几条就拆几个。'),
[qc('拆微服务最合理的时机是？',['项目一开始就拆','出现协作冲突、独立扩容或发布节奏差异等痛点时','团队超过三个人就拆','为了用上新技术'],1,'拆分是用复杂度换解耦，痛点不明确时收益覆盖不了代价。')],
'<p>那具体该怎么拆？→ <a href="#/l/microservice/02-split">下一课：怎么拆才不会拆坏</a></p>'
],
['02-split','怎么拆才不会拆坏',6,['领域边界','拆分粒度','数据库'],
'<p>一句话：按<b>业务领域</b>拆而不是按技术层次拆，且每个服务应该拥有自己的数据——按"技术层"拆出来的服务会互相纠缠，改一处要动全部。</p>',
'<p>最常见的错误是按技术分层拆：一个专门操作数据库的服务、一个专门做业务逻辑的服务、一个专门做前端的服务。结果是任何需求都要同时改三层，网络调用翻倍，完全没有解耦。正确做法是按业务能力拆：订单、支付、库存、用户各自成服务，每个服务内部自己管数据库、自己管逻辑，对外只暴露接口。</p><p>第二个关键原则是<b>数据私有</b>：订单服务不直接读支付服务的库，只能调它的接口。共享数据库看起来省事，实际上把两个服务又焊在了一起，将来想替换任何一个都动不了。跨服务的数据需求要用接口、事件或数据同步来满足，这是拆分必须付的成本。</p>'+ex('反例很典型：三个服务共用一个数据库，某次给一张表加字段，三个服务的代码都得改、得同时发版，等于拆了但白拆。改成各自持有数据、通过接口协作后，改动的爆炸半径才真正缩小。'),
pit('别为了"服务小"而拆得过细。粒度太细会让一次业务请求跨十几次网络调用，延迟与失败概率都上升，运维成本也翻倍。合理的粒度是"一个团队能完整负责一个服务"。'),
[qc('微服务拆分中"数据私有"的含义是？',['每个服务都要用不同数据库产品','服务只能通过接口访问别人的数据，不直接读别人的库','数据要加密','数据必须分布式存储'],1,'数据私有是解耦的关键，共享库会让服务之间的边界形同虚设。')],
'<p>服务多了，调用方怎么找到它们？→ <a href="#/l/microservice/03-discovery">下一课：服务发现</a></p>'
],
['03-discovery','服务发现：谁在哪',6,['服务发现','注册中心','健康检查'],
'<p>一句话：'+g('service-discovery','服务发现')+'让服务实例启动时自动登记、下线时自动摘除，调用方按服务名查地址，不用手工维护 IP 清单。</p>',
'<p>在容器环境里，实例的 IP 与数量随时在变，把地址写进配置文件根本活不下去。服务发现通常由注册中心承担：实例启动后向注册中心登记"我是订单服务的某个实例，地址是 X"，注册中心定期健康检查，调用方查询服务名得到可用实例列表，再选一个发起调用。整个过程自动完成，扩容缩容对调用方透明。</p><p>Kubernetes 环境里这一步通常由平台内置能力完成——Service 与集群 DNS 让"按服务名访问"成为默认行为，不需要额外引注册中心。非容器的老系统则常用专门的注册中心组件。区别只是实现，原理都是"动态名册 + 健康检查"。</p>'+ex('订单服务从 4 个副本扩到 10 个副本，调用方不需要任何配置变更就能把流量分到新实例；某个实例崩溃被摘除后也不再收到请求——这就是"名册自动更新"的价值。'),
pit('别在客户端缓存服务地址太久。长时间不刷新会持续往已下线的实例发请求，表现为间歇性超时。客户端要么定期刷新，要么交给平台层统一处理。'),
[qc('服务发现要解决的核心问题是？',['加密通信','实例地址动态变化时调用方仍能找到可用实例','限制流量','备份数据'],1,'名册随实例变化自动更新，调用方只认服务名，这是本质。')],
'<p>所有请求都直连服务合适吗？→ <a href="#/l/microservice/04-gateway">下一课：API 网关</a></p>'
],
['04-gateway','API 网关：统一入口',6,['api网关','鉴权','限流','路由'],
'<p>一句话：'+g('api-gateway','API 网关')+'是所有外部请求的统一入口，把鉴权、限流、路由、日志这些横切关注点集中在一处，而不是让每个服务各写一遍。</p>',
'<p>没有网关时，每个服务都要自己处理令牌校验、防刷限流、跨域、灰度路由，代码重复且版本不一，安全策略也很难统一。有了网关，外部请求先到它这里：核对身份、判断有没有超限、按路径转发给对应服务，然后把结果返回。服务只关心业务逻辑。</p><p>网关也是<b>内外边界</b>：只有它对外暴露，内部服务只在集群内可达，攻击面显著缩小。不过它天然是流量的必经之路，所以它自身的高可用与性能必须过硬——网关故障等于全站故障，通常要部署多副本并做容量评估。</p>'+ex('一次安全整改：把所有对外接口的鉴权从各服务里收拢到网关统一做，顺带加上了按用户与接口维度的限流。服务代码删掉大量重复逻辑，安全策略也终于能一处修改全局生效。'),
pit('别把业务逻辑写进网关。网关应该是"薄"的通道，一旦在里面塞了业务规则，它就会变成新的单体，每次业务改动都要动网关、都要全站发布，把解耦成果又还了回去。'),
[qc('把鉴权与限流放在网关的主要收益是？',['让网关更忙','横切能力集中管理、服务代码不重复、内外边界清晰','提高数据库性能','减少服务数量'],1,'统一入口让安全与治理策略一处生效，同时收窄对外暴露面。')],
'<p>配置怎么管才不会到处是副本？→ <a href="#/l/microservice/05-config">下一课：配置中心</a></p>'
],
['05-config','配置中心与动态生效',6,['配置中心','动态刷新','环境隔离'],
'<p>一句话：'+g('config-center','配置中心')+'集中存放各服务的配置，支持按环境隔离与动态刷新，改配置不必重新打包发布。</p>',
'<p>配置散落在各服务的配置文件、启动参数或环境变量里，会带来三个麻烦：多环境容易混（测试地址被带到生产）、改一个阈值要走完整发布流程、出问题时不知道当前生效的值是什么。配置中心把配置集中起来，按"应用 + 环境 + 版本"管理，服务启动时拉取，运行时监听变更。</p><p>它带来的实际收益很直接：把开关一关就能停掉某个功能、调整限流阈值不用发版、灰度配置只对新版本生效。但要知道<b>动态刷新不是免费的</b>——应用要支持重新读取，配置错误会被瞬间推到全部实例，所以配置变更同样需要审核与灰度。</p>'+ex('大促前把限流阈值从常规值调高，通过配置中心推给全部实例即时生效；活动结束后再推回原值。整个过程没有一次代码发布，也没有重启服务。'),
pit('别把配置中心当成"随便改"的地方。一次误改可能让全部实例同时读错参数，影响面比一次代码发布更大。生产配置要有审核、有版本记录、有回滚手段，敏感项继续放密钥管理。'),
[qc('配置中心相对配置文件的最大优势是？',['配置更少','集中管理、按环境隔离、可动态生效且可追溯','启动更快','不需要重启服务'],1,'集中加动态，让配置变更从"发版动作"变成"可控的运维动作"。')],
'<p>如果下游突然变慢或挂掉呢？→ <a href="#/l/microservice/06-resilience">下一课：熔断、限流与降级</a></p>'
],
['06-resilience','熔断、限流与降级',7,['熔断','限流','降级','雪崩'],
'<p>一句话：'+g('circuit-breaker','熔断限流降级')+'是分布式系统的三道保险——限流挡住过量请求、熔断快速切断坏掉的依赖、降级用兜底内容保住主干功能。</p>',
'<p>三个手段针对不同问题：<b>限流</b>控制入口速率，把系统保护在承载能力之内（令牌桶、漏桶、并发数限制）；<b>熔断</b>监测下游失败率，超过阈值就暂时不再调用，直接返回失败或兜底——避免大量线程被慢调用占满；<b>降级</b>是在资源不足或依赖不可用时，主动关闭非核心功能（推荐位、排行榜），把资源留给下单支付这类主干。</p><p>没有这些保护，一个小依赖的抖动会沿着调用链放大：线程被慢请求占满、上游开始超时、上游的上游也超时，最终整个系统雪崩。所以超时、重试、熔断、隔离这几项要一起配，而且<b>超时时间必须逐层递减</b>，否则最外层早就超时了，内层还在傻等。</p>'+ex('推荐服务响应从 50ms 恶化到 5s。因为有熔断与超时保护，商品详情页只是少了个推荐位、其余正常；如果没有保护，请求线程被拖住，整个详情页会一起变慢直至不可用。'),
pit('别把重试当成万能药。失败时无脑重试会把下游压力翻几倍，反而加速崩溃；重试必须配合指数退避、次数上限与熔断，并且只对幂等操作重试。'),
[qc('熔断器触发后通常的行为是？',['继续调用直到超时','快速失败或返回兜底，给下游恢复时间','自动扩容下游','重启下游服务'],1,'熔断的本质是"快速失败 + 保护"，让故障不再继续放大。')],
'<p>调用链一长，出问题怎么定位？→ <a href="#/l/microservice/07-trace-tx">下一课：链路追踪与分布式事务</a></p>'
],
['07-trace-tx','链路追踪与分布式事务',7,['链路追踪','分布式事务','最终一致'],
'<p>一句话：'+g('trace','链路追踪')+'给一次请求打上全局标识，把跨服务的调用串成一条完整轨迹；'+g('distributed-tx','分布式事务')+'则要解决"多个服务都成功才叫成功"的难题，实践中多用最终一致加补偿。</p>',
'<p>单体时代排查靠一份日志；拆成微服务后，一次下单要经过网关、订单、库存、支付、通知五个服务，日志分散在五处。链路追踪的做法是在入口生成唯一的 trace ID，随调用一路传递，每个服务都把它写进日志与上报，于是可以按 ID 把整条路径、每段耗时、每个错误都拉出来。它和指标、日志一起构成可观测性三支柱。</p><p>分布式事务更棘手：跨服务的操作没法用一个数据库事务包住。强一致方案（两阶段提交）代价高、性能差，实践中更常用<b>最终一致</b>——先记录本地事务与待发消息（事件表或消息队列），成功后异步通知对方，失败则重试或反向补偿（如扣减失败要回滚已扣的库存）。关键是每个环节都要<b>幂等</b>，重复执行不会造成二次扣减。</p>'+ex('下单成功但发券失败：系统记录一笔待补偿任务，后台定时重试发券；若最终确认失败，则触发退款或人工介入。用户看到的"下单成功"不会因为下游故障而回滚，但账面最终会对齐。'),
pit('别指望"分布式事务框架装上就万事大吉"。真正的难点在于业务补偿逻辑与幂等设计——没有幂等的接口，任何重试都会制造重复扣款。先把"重复执行会怎样"想清楚，再谈一致性方案。'),
[qc('跨服务一致性最常用的做法是？',['全部使用两阶段提交','最终一致 + 重试/补偿 + 接口幂等','不做任何一致性保证','把所有服务合回单体'],1,'强一致代价高，工程上普遍用最终一致配合幂等与补偿来达成业务正确性。')],
'<p>服务能跑、能协作了，还要能高效交付。→ <a href="#/m/devops">下一模块：DevOps 与流水线</a></p>'
]
];

/* 模块 8:DevOps 与流水线(7 课) */
L.devops = [
['01-culture','DevOps 到底是什么',6,['devops','协作','责任共担'],
'<p>一句话：'+g('devops','DevOps')+'不是某个工具，而是开发与运维共同为"交付速度与线上稳定"负责的协作方式——工具只是落地手段。</p>',
'<p>传统分工下，开发的目标是"尽快把功能交出去"，运维的目标是"尽量别出事"，于是天然对立：开发写完甩给运维、运维上线时小心翼翼、出事互相甩锅。DevOps 把这两件事绑到同一个目标上：交付要快，也要稳，且由同一批人对结果负责。</p><p>落地时通常体现在几件事上：代码与配置都进版本库、构建测试部署全部自动化、监控与告警对开发开放、故障复盘不追责而是改流程。看一个团队是不是真的在做 DevOps，看它"上线要多久、回滚要多久、谁值班看告警"就够了。</p>'+ex('衡量效果的两个直观指标：从提交代码到上生产的时间（越短越好），以及一次故障从发现到恢复的时间。工具怎么换都可以，这两个数字不改善，说明协作方式没变。'),
pit('别把"买了 CI/CD 工具"当成做了 DevOps。工具能把流程自动化，但如果开发仍然写完就不管、运维仍然独自扛告警，那只是把老流程跑得更快而已，问题会以更快的速度暴露出来。'),
[qc('DevOps 的核心变化是？',['把运维外包','开发与运维共同对交付速度与线上稳定负责','取消运维岗位','全部使用云服务'],1,'职责与目标统一，是 DevOps 的本质；自动化工具是达成手段。')],
'<p>第一步总是从代码管理开始。→ <a href="#/l/devops/02-git">下一课：分支策略与代码评审</a></p>'
],
['02-git','分支策略与代码评审',6,['分支策略','代码评审','主干'],
'<p>一句话：分支策略决定"代码怎么合并、什么时候能上生产"，主流趋势是<b>主干开发 + 短分支 + 强制评审</b>，而不是长期并存多个分支。</p>',
'<p>长期分支（如开发分支、测试分支、发布分支各存几周）会带来一个大问题：合并冲突在最后时刻集中爆发，谁也不敢合。短分支 + 频繁合并主干，冲突小、可回溯。配套的关键动作是<b>代码评审</b>：任何改动都要有人看过才能合入，评审能抓到的问题远比测试多——命名、边界、异常处理、安全隐患。</p><p>还要用分支保护规则把纪律固化下来：主干禁止直接推送、必须通过流水线检查与评审、必须能追溯到对应的需求或缺陷单。这些不是为了限制人，而是让"能在任何时候发布"成为常态。</p>'+ex('一次事故复盘发现：问题代码是深夜直接推送到主干的，绕过了评审与测试。之后把主干设为保护分支、禁止直推并强制流水线通过，同类问题再没出现过。'),
pit('别让"这个改动很小，先合了再补评审"成为习惯。绕过流程的小改动占事故起因的比例远高于大改动；流程的价值恰恰体现在小改动最容易偷懒的地方。'),
[qc('短分支频繁合并主干的主要好处是？',['显得代码量多','冲突小、随时可发布、可回溯','不用写测试','不用评审'],1,'长期分支会把冲突与风险积压到发布前夜，短分支把风险摊平到日常。')],
'<p>每次提交后自动跑什么？→ <a href="#/l/devops/03-ci">下一课：持续集成</a></p>'
],
['03-ci','持续集成：一次提交自动跑完',6,['持续集成','流水线','质量门禁'],
'<p>一句话：'+g('ci-cd','持续集成')+'是每次提交代码都自动构建、跑测试、做检查，让问题在几分钟内就被发现，而不是在发布前夜。</p>',
'<p>没有持续集成时，几个人各自开发几周再合并，等于把冲突与缺陷堆到一起，一次性爆发且极难定位。持续集成把反馈周期压缩到分钟级：提交后自动编译、跑单元测试、做静态检查、扫描依赖漏洞、构建镜像，任何一步失败立刻通知提交者。</p><p>要让这套流程真正有用，得满足几个条件：<b>跑得快</b>（几分钟内出结果，否则没人等）、<b>结果可信</b>（测试稳定，不能时好时坏）、<b>失败就修</b>（红了的流水线要立刻恢复，不能长期挂着）。流水线的意义在于<b>质量门禁</b>：不合格的产物根本进不了后面环节。</p>'+ex('一片流水线的典型阶段：拉代码与依赖缓存、单元测试、静态扫描、构建镜像并推仓库。全程五分钟左右，提交者在喝完一杯咖啡前就知道自己有没有搞坏什么。'),
pit('别让不稳定测试长期留在流水线里。时好时坏的测试会让所有人养成"失败了重跑一次"的习惯，门禁形同虚设——发现不稳定用例就该立刻隔离修复，而不是重试掩盖。'),
[qc('持续集成最重要的价值是？',['少写测试','把缺陷发现时间从发布前压到提交后几分钟','让代码更好看','不用评审'],1,'缩短反馈周期，是持续集成所有收益的源头。')],
'<p>流水线产出的东西放哪、怎么保证一致？→ <a href="#/l/devops/04-artifact">下一课：制品与环境一致</a></p>'
],
['04-artifact','制品与环境一致',6,['制品库','不可变制品','环境一致'],
'<p>一句话：'+g('artifact','制品库')+'存放构建产物（包、镜像），并且要做到"一次构建、处处部署"——同一份制品从测试到生产都不重新构建。</p>',
'<p>常见的坏做法是每个环境各自构建一次：测试环境用今天的代码构建，生产上线的早上再构建一次。哪怕代码没变，依赖版本、构建工具版本、时间戳都可能不同，"测试通过、生产出问题"就这样产生了。</p><p>正确做法是把制品视为<b>不可变</b>：构建一次，打上唯一版本号（提交哈希最理想），之后测试、预发、生产都部署这同一个制品，环境差异只通过外部配置体现。这样出问题时可以确信"跑的确实是同一个东西"，而回滚也只需要指定上一个版本。</p>'+ex('一次诡异的线上问题：测试环境一切正常，生产报错。追查发现是两次构建拉到了不同的依赖小版本。改为"一次构建、按版本晋级"之后，这类问题基本消失。'),
pit('别在生产环境重新构建镜像。哪怕只是"改个配置重新打一次包"，也会引入不可控差异。配置要外置，制品要复用，这是发布可预期的前提。'),
[qc('"一次构建、处处部署"的核心目的是？',['省时间','保证各环境跑的是同一份制品，消除构建差异带来的问题','省存储','减少镜像数量'],1,'制品不可变让环境差异只剩配置，问题定位与回滚都变得确定。')],
'<p>有了制品，怎么安全地上线？→ <a href="#/l/devops/05-cd-strategy">下一课：蓝绿、灰度与回滚</a></p>'
],
['05-cd-strategy','蓝绿、灰度与回滚',7,['蓝绿','灰度','金丝雀','回滚'],
'<p>一句话：'+g('canary','灰度发布')+'先把少量流量切给新版本验证，'+g('blue-green','蓝绿发布')+'则准备整套新环境后一次性切换——两者的共同目标是让发布可回退、可控制。</p>',
'<p><b>灰度（金丝雀）</b>：新版本先接 1% 甚至 0.1% 的真实流量，观察错误率、延迟、业务指标，正常再逐步放量。适合用户量大、难以完全预演的场景。关键在于<b>放量要可控、指标要可比</b>，而且新旧版本要能同时运行。<b>蓝绿</b>：长期维护两套完全一样的环境，新版在备用环境部署验证完毕，切换入口即完成上线；出问题切回来就行，恢复极快。代价是资源要备两套。</p><p>无论哪种方案，<b>可回滚</b>都是底线：数据库变更尤其要考虑兼容——通常采用"先加字段、再改代码、最后清理"的渐进式变更，保证任何时刻新旧版本都能跑。</p>'+ex('一次发布把新版本以 5% 流量接入，监控发现下单转化率轻微下降，立即关闭灰度：全程只影响少量用户，也没有回滚数据库的必要，十几分钟后问题定位到一次依赖升级。'),
pit('别在发布的同时做不兼容的数据库变更。新代码上线而表结构还没改完，或者改了结构导致旧版本读不了，都会造成"回滚也救不回来"的局面。数据库变更与代码发布必须解耦。'),
[qc('灰度发布最关键的两个条件是？',['流量可控 + 新旧版本能同时运行','代码量小 + 团队人少','数据库变更 + 停机维护','只用新用户验证'],0,'没有可控流量和并行运行能力，灰度就无从谈起。')],
'<p>如果环境本身也是自动创建的呢？→ <a href="#/l/devops/06-iac">下一课：基础设施即代码</a></p>'
],
['06-iac','基础设施即代码',6,['iac','自动化','可复现'],
'<p>一句话：'+g('iac','基础设施即代码')+'把服务器、网络、数据库这些资源的定义写成代码文件，用工具自动创建与核对——环境不再靠人手点，而是可以被复现、被评审、被版本管理。</p>',
'<p>手工在控制台上点出来的环境有三个致命问题：<b>不可复现</b>（重建一次就得重新点一遍，还容易漏）、<b>不可追溯</b>（谁在什么时候改了哪个参数没人知道）、<b>不可评审</b>（变更没有经过同行检查）。写成代码后，环境定义进版本库，变更走评审与流水线，随时可以重新创建一套一致的环境。</p><p>更重要的能力是<b>漂移检测</b>：有人手工改了一个安全组规则，代码执行时能发现实际状态与声明不一致并纠正（或报警）。这让"线上配置和文档不一致"这种经典问题有了系统性解法。</p>'+ex('需要一套完整的演示环境：跑一次脚本，网络、子网、数据库、负载均衡、集群全部自动创建，二十分钟后可用；演示结束一条命令全部销毁，不留下计费资源。'),
pit('别把 IaC 只用在"从零创建"。真正的价值在于<b>持续对齐</b>：既有环境也要纳入管理，定期执行检查漂移，否则手工改动会让代码描述与实际状态越来越远，最后谁也不敢动。'),
[qc('基础设施即代码带来的关键能力是？',['让环境更快','环境可复现、变更可评审、能检测并纠正漂移','省掉运维人员','不再需要监控'],1,'声明式管理让环境变成可版本化、可复核的资产。')],
'<p>如果连"部署什么"也由代码仓库说了算呢？→ <a href="#/l/devops/07-gitops">下一课：GitOps</a></p>'
],
['07-gitops','GitOps：以仓库为准',7,['gitops','声明式','自动同步'],
'<p>一句话：'+g('gitops','GitOps')+'把 Git 仓库当作系统期望状态唯一的来源，集群里跑的东西由仓库决定，有人改了集群也会被自动纠回仓库描述的样子。</p>',
'<p>它把"声明式配置"和"自动化同步"结合：仓库里放着一份描述（要哪些服务、什么版本、多少副本），集群里有一个同步程序持续比对实际状态；一旦发现不一致就拉齐。发布新版本不再是"人工执行部署命令"，而是"修改仓库里的版本号并合并"——发布记录就是提交历史，回滚就是回退提交。</p><p>这套模式带来两个明显好处：<b>审计天然完整</b>（谁在什么时候把什么改成了什么，全在提交记录里）、<b>权限收敛</b>（不需要给所有人集群操作权限，只要控制仓库合并权限）。有正式合规要求的场景尤其受用。</p>'+ex('一次误操作把线上副本数改成了 0，几分钟后同步程序发现实际状态与仓库声明不符，自动把副本恢复——人的操作失误被系统纠偏机制兜住了。'),
pit('别让"仓库描述"和"手工改动"长期共存。只要允许绕过仓库直接改集群，就会形成两套事实来源，同步程序还会把你的临时改动悄悄回滚，让人困惑。规则要统一：一切变更走仓库。'),
[qc('GitOps 中"回滚"通常意味着？',['重启集群','回退 Git 提交，让同步程序把集群改回旧状态','恢复备份','手工改配置'],1,'仓库是唯一事实源，回滚就是版本回退，随后自动收敛。')],
'<p>交付快了，怎么知道线上到底好不好？→ <a href="#/m/observability">下一模块：可观测性</a></p>'
]
];
