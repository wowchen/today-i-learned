/* 模块 14:云上 AI 平台(7 课) */
L['ai-platform'] = [
['01-why','为什么需要 AI 平台',6,['ai平台','算力','标准化'],
'<p>一句话：'+g('ai-platform','AI 平台')+'把"抢算力、配环境、跑任务、发模型"这四件重复劳动标准化——没有它，每个团队都在用脚本和表格管昂贵的加速卡。</p>',
'<p>痛点非常具体：加速卡贵且稀缺，谁先用谁后用得靠人协调；训练环境依赖复杂，换一台机器就跑不起来；任务断了要从头再跑几小时；模型上线靠研究员手把手教运维，版本对不上就回滚不了。平台把这四件事变成流程：<b>算力排队与配额</b>、<b>环境与镜像标准化</b>、<b>任务编排与断点续训</b>、<b>模型仓库与发布流水线</b>。</p><p>它带来的收益也很直白：算力利用率上升（从各自独占变成共享调度）、等待时间下降、实验可复现、上线可回滚。对以 AI 为核心竞争力的团队，平台的价值往往比单个模型更大——因为模型会迭代，流程会沉淀。</p>'+ex('两个团队的做法对比：没有平台时，研究员各自申请机器、手写启动脚本、模型文件放在自己的目录里；有平台后，提交任务走队列、环境来自统一镜像、产出模型自动登记版本并附带训练数据与参数，任何人都能复现。'),
pit('别把平台建成"又一个审批系统"。平台的目的是降低摩擦而不是增加流程，如果一张卡要三天审批、一个实验要填五张表，团队会立刻绕开它用回自己的脚本——那时平台的投入就白费了。'),
[qc('AI 平台最核心的价值是？',['让模型更准','把算力、环境、任务与模型发布标准化，提高效率与可复现性','替代研究员','降低显卡价格'],1,'平台不产出模型，它产出的是"高效且可复现的研发过程"。')],
'<p>先看最贵的资源怎么管。→ <a href="#/l/ai-platform/02-gpu-pool">下一课：GPU 资源池与调度</a></p>'
],
['02-gpu-pool','GPU 资源池与调度',7,['gpu池','切分','队列'],
'<p>一句话：'+g('gpu-pool','GPU 资源池')+'把散在各处的加速卡汇成共享池，靠队列、配额、优先级来分配——算力越贵，调度越值钱。</p>',
'<p>池化之后要解决三个矛盾：<b>独占与共享</b>——训练通常需要整卡甚至多卡，而小规模推理只用一小部分，于是有了 '+g('gpu-partition','GPU 切分')+'（按显存或计算份额切给多个任务）；<b>长任务与短任务</b>——一个训练要跑几十小时，一个调参只要几分钟，混在一起跑会让短任务排队到天荒地老，所以要用<b>队列 + 优先级 + 抢占</b>；<b>保障与弹性</b>——给关键项目留配额，其余资源按需竞争。</p><p>调度策略上的常见取舍是"<b>整卡独占优先保证性能，碎片时间用切分填满</b>"。另外要考虑<b>多样性</b>：不同型号的卡在显存与算子支持上有差异，平台需要能按型号调度，并让用户声明所需规格，否则会出现"任务分到了卡但跑不起来"。</p>'+ex('一个典型的调度规则：训练任务要求整卡、优先级高、可被抢占但会重排队；推理任务允许切分、常驻不被抢占；调试任务走最低优先级、限定最长两小时。几套规则组合后，同样的卡数能支撑的任务量明显增加。'),
pit('别忘了网络与存储也是瓶颈。多机多卡训练对节点间带宽极敏感，万兆网络下的分布式训练可能越训越慢；数据集读取慢同样会把昂贵算力晾在一边。调度时要把网络与存储条件一起纳入，而不是只数卡。'),
[qc('多机多卡训练中常被忽略的瓶颈是？',['显卡驱动版本','节点间网络带宽与数据读取速度','操作系统语言','机柜颜色'],1,'算力再强，被网络与存储卡住就只能空转——这也是"算力利用率"问题的常见来源。')],
'<p>卡有了，怎么把训练任务跑稳？→ <a href="#/l/ai-platform/03-training">下一课：训练任务的编排与容错</a></p>'
],
['03-training','训练任务的编排与容错',7,['训练任务','断点续训','checkpoint'],
'<p>一句话：'+g('training-job','训练任务')+'在平台上是一个可编排、可恢复、可追溯的作业——关键能力是'+g('checkpoint','断点续训')+'，因为长任务必然遇到中断。</p>',
'<p>一个训练跑几十小时，中断原因五花八门：硬件故障、节点被抢占、电源或网络波动、代码里有个内存泄漏跑久了才炸。没有断点续训，每次都从头跑，几天时间就白费了。所以平台强制要求：<b>定期保存检查点</b>（模型权重 + 优化器状态），中断后能从最近一次继续；<b>输出与日志落到共享存储</b>，任务结束或崩溃都能取回；<b>参数与数据版本登记</b>，保证这次实验可复现。</p><p>编排层面还要解决分布式训练的一致性：多机多卡需要同步启动、统一随机种子、正确处理某个节点掉线的情形。这些细节单靠脚本维护极难，平台把它们变成任务模板里的标准配置，团队只需声明规模与资源。</p>'+ex('一次真实场景：32 卡训练在第 18 小时被节点故障打断，因为每 30 分钟存一次检查点，任务自动在新节点上恢复，只损失了不到半小时的进度；若没有这套机制，前面 18 小时全部作废。'),
pit('别把检查点只当"保险"。它同时是实验管理的一部分：检查点与数据版本、超参数、代码提交号绑在一起，才能在几周后回答"这个效果到底是怎么跑出来的"。缺少这层绑定，团队会陷入无法复现的泥潭。'),
[qc('长时训练任务最关键的平台能力是？',['更多显卡','定期保存检查点并能自动恢复','更大的磁盘','更快的网络'],1,'长任务必然中断，能否从中断处继续，直接决定算力是否被浪费。')],
'<p>训好之后怎么对外提供服务？→ <a href="#/l/ai-platform/04-inference">下一课：推理服务化</a></p>'
],
['04-inference','推理服务化',7,['推理','模型服务','批处理'],
'<p>一句话：'+g('inference','推理')+'服务化是把模型变成稳定的接口——同样的模型，在线按请求响应、离线批量跑数据，两种形态的优化目标完全不同。</p>',
'<p>在线推理关注<b>延迟与并发</b>：请求进来要在一两百毫秒内返回，所以要控制首包延迟、支持并发、能按流量自动扩缩；常用手段包括模型量化压缩、批处理合并请求、预热避免冷启动、多副本负载均衡。离线推理（'+g('batch-inference','批处理推理')+'）关注<b>吞吐与成本</b>：几千万条数据排队跑几小时没关系，但单位成本要低，通常放在电价低谷或算力空闲时段执行。</p><p>工程上还要考虑版本与灰度：新模型上线要能双版本并行、按流量比例切分、指标不达标快速回退。推理监控也不能只看接口成功率，还要看<b>业务指标与数据漂移</b>——接口全都 200，但预测结果整体偏移了，才是最难发现的问题。</p>'+ex('一个可落地的分工：面向用户的接口用在线推理小规格多副本，保证响应；每晚运行批量任务为全量用户刷新推荐结果，用低优先级算力排队执行。同一模型，两种部署形态各司其职。'),
pit('别拿离线测试的精度去承诺线上效果。线上会遇到数据分布变化、异常输入、并发压力，务必灰度放量与持续监控；同时给推理服务设置超时与降级策略，模型服务不可用时返回兜底结果而不是报错。'),
[qc('在线推理与离线推理的优化目标分别是？',['都一样，都是越快越好','在线追求低延迟高并发，离线追求低单位成本高吞吐','在线追求成本，离线追求延迟','取决于模型大小'],1,'两种形态的服务目标不同，因此扩容、量化与调度策略也不同。')],
'<p>模型越训越多，怎么管得住？→ <a href="#/l/ai-platform/05-model-data">下一课：模型与数据管理</a></p>'
],
['05-model-data','模型与数据管理',7,['模型仓库','数据血缘','评测'],
'<p>一句话：'+g('model-registry','模型仓库')+'给每个模型版本建立档案（来源、参数、指标、依赖），'+g('data-lineage','数据血缘')+'回答"这份数据从哪来、喂给了哪些模型"。</p>',
'<p>没有管理时，团队里的模型文件靠命名区分：model_final、model_final_v2、model_final_真最终版。谁都不敢删，谁也不知道哪个在线。模型仓库的做法是给每次产出登记版本号，并绑定训练数据版本、超参数、代码提交号与评测结果，形成可追溯的链条。上线时引用具体版本号，回滚就是切回上一个版本号。</p><p>数据侧同样要有账本：数据集从哪些源表来、经过了什么清洗、被哪些任务消费。这在合规审查与故障排查时格外关键——一旦发现某个源数据有问题，血缘能立刻告诉你"影响了哪些模型、哪些报表"。再加上定期用固定测试集做 '+g('model-eval','评测')+'，才能判断新版本是进步还是退步。</p>'+ex('一次排查：某模型线上效果下滑，靠血缘查到它依赖的一份数据源在两周前改了清洗规则，导致特征分布变化。定位时间从"挨个翻代码"缩短到十几分钟。'),
pit('别只登记模型，忽略数据与评测。模型是结果，数据与参数才是原因；没有评测基线，所谓"新版本更好"就只是感觉。评测集要固定且隔离，避免用线上数据反复调参造成虚假的提升。'),
[qc('模型仓库除了存文件，更重要的作用是？',['节省磁盘','记录模型版本与其来源数据、参数、指标，做到可追溯可回滚','加密模型','压缩模型体积'],1,'可追溯与可回滚，是模型管理中真正决定工程效率的部分。')],
'<p>算力这么贵，怎么把利用率提上去？→ <a href="#/l/ai-platform/06-cost">下一课：算力利用率与成本治理</a></p>'
],
['06-cost','算力利用率与成本治理',7,['利用率','成本','错峰'],
'<p>一句话：'+g('gpu-utilization','算力利用率')+'是 AI 成本的第一变量——同一批卡，利用率从三成提到七成，单位产出的成本就砍掉一半以上。</p>',
'<p>浪费来自几处典型场景：<b>占了不用</b>（调试完忘了释放，卡被独占却闲置）、<b>用了不满</b>（小模型跑整卡、批次太小填不满显存）、<b>反复重跑</b>（没有断点续训、环境不稳定）、<b>规格错配</b>（推理用训练规格）。治理手段也对应这四类：闲置超时自动回收、按需切分与批处理优化、任务模板与检查点标准化、推理与训练资源分池。</p><p>成本上还有两层：<b>采购形态</b>——按量、预留、包年与自建各有适用场景，长期稳定的训练负载用预留往往能省下可观比例；<b>时间维度</b>——可延迟任务安排在电价低谷与绿电富余时段执行，就是前面讲过的算电协同思路在平台侧的落地。</p>'+ex('一次利用率整改：开启闲置两小时自动释放、把调试任务限制在切分规格、给推理服务调大批次参数。同样的卡数，月度可完成任务量提升近一倍，账单没有增加。'),
pit('别只盯着单卡单价谈判，而忽略利用率与闲置。买得便宜但三成利用率的资源，实际单位成本远高于贵一点但七成利用率的方案；同时也别为了追求利用率把关键训练压到低优先级，导致交付延期。'),
[qc('提高算力投资回报最直接的手段是？',['换更贵的卡','提高利用率：回收闲置、切分规格、标准化任务减少重跑','增加存储容量','更换机房'],1,'利用率决定了同一批硬件的实际产出，是最先该治理的变量。')],
'<p>最后一课看平台上最热的落地形态。→ <a href="#/l/ai-platform/07-rag-agent">下一课：平台上的 RAG 与智能体</a></p>'
],
['07-rag-agent','平台上的 RAG 与智能体',7,['rag','向量库','智能体','评测'],
'<p>一句话：'+g('ai-platform','AI 平台')+'的另一半价值在于承载应用层——把'+g('vector-db','向量数据库')+'、检索增强生成（RAG）与智能体流程做成可复用组件，而不是每个项目重写一遍。</p>',
'<p>企业落地最常见的是 RAG：把内部文档切块、向量化后存入向量库，用户提问时先检索相关片段再交给模型生成答案，从而让回答有据可依、可追溯来源。平台化的收益在于<b>把公共部分沉淀</b>：文档解析与切分策略、向量化与索引更新、检索参数与重排、以及带引用来源的输出格式。更进一步是智能体：让模型按流程调工具、查系统、走审批，把"问答"变成"办事"。</p><p>两条底线必须写进流程：一是<b>权限</b>——检索结果要按用户权限过滤，绝不能出现"某员工问出了财务数据"；二是<b>评测</b>——用固定问题集回归测试，关注答案正确率、引用命中率与拒答率，否则改一次提示词就可能悄悄变差。</p>'+ex('一个实用的验收方式：准备 100 个真实业务问题（含 20 个应拒答的越权或超范围问题），每次版本更新都跑一遍并对比指标。这个测试集比任何主观"感觉更聪明"都可靠。'),
pit('别把 RAG 的效果全押在换模型上。实际瓶颈多在文档切分粒度、检索召回与权限过滤；把这几处做扎实，往往比升级模型带来更明显的提升，而且成本更低。'),
[qc('企业 RAG 落地时最容易被忽视的两条底线是？',['模型参数规模、训练数据量','按用户权限过滤检索结果、用固定问题集做回归评测','界面美观、响应速度','显卡型号、机房位置'],1,'越权泄露与效果退化是 RAG 最常见的两类事故，需要制度和流程来防。')],
'<p>平台与算力讲完，最后回到项目层面看怎么落地。→ <a href="#/m/practice">下一模块：上云实战与选型</a></p>'
]
];
