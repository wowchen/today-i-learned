/* container/05-registry (自动生成) */
CCN.registerLesson({"id":"container/05-registry","module":"container","order":5,"title":"镜像仓库与分发","minutes":5,"keywords":["registry","镜像仓库","标签","分发"],"concept":"<p>一句话：<gd data-term=\"registry\">镜像仓库</gd>是集中存放与分发镜像的地方，用\"仓库名 + 标签\"定位具体版本，集群每一台机器都从这里拉取。</p>","core":"<p>它的作用类似代码仓库，但存的是构建产物。命名通常包含注册地址、项目、镜像名与标签，例如 registry.example.com/pay/service:v1.2.3。标签策略是重点：<b>用不可变的版本号或提交号</b>（v1.2.3、git 短哈希）而不要用 latest 或 \"test\" 这类会漂移的标签；生产环境甚至可以按<b>镜像摘要</b>部署，确保拉到的字节完全一致。</p><p>仓库分公共与私有：公共镜像方便取用基础镜像，但企业内部代码必须放私有仓库，并配好登录凭据与拉取权限。大集群还要考虑就近拉取、镜像预热与清理策略，否则\"镜像拉不下来\"会成为发布失败的常见原因。</p><div class=\"ex\">发布失败排查的一个常见分支：代码全对，但目标节点拉镜像超时或认证失败。把镜像仓库的连通性与凭据加入发布前检查清单，能省掉大量深夜排查。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别在生产环境用 latest 标签。更危险的是\"用同一个标签覆盖推送新内容\"——同一份配置在不同时间部署出不同版本，回滚时也不知道该回到哪一版。标签要么不可变，要么别用在生产。</div>","quiz":[{"type":"choice","q":"生产部署推荐用什么标识镜像版本？","options":["latest","同名的可覆盖标签","不可变的版本号或提交哈希","构建时的日期随便写"],"answer":2,"explain":"不可变标签让\"同一版本部署出的结果一致\"，也让回滚有明确目标。","source":"想一想"}],"links":"<p>仓库里的镜像，是谁把它跑起来的？→ <a href=\"#/l/container/06-runtime\">下一课：容器运行时</a></p>"});
