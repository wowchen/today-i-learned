/* container/04-dockerfile (自动生成) */
CCN.registerLesson({"id":"container/04-dockerfile","module":"container","order":4,"title":"Dockerfile 与构建","minutes":7,"keywords":["dockerfile","构建","多阶段"],"concept":"<p>一句话：<gd data-term=\"dockerfile\">Dockerfile</gd>是一份文本文配方，按顺序描述\"从哪个基础镜像开始、装什么、拷什么、怎么启动\"，构建工具据此产出镜像。</p>","core":"<p>几条被反复验证的写法：<b>选精简基础镜像</b>（够用就好，越小的镜像拉取越快、漏洞越少）；<b>先装依赖再拷代码</b>（利用层缓存）；<b>用多阶段构建</b>（编译阶段用带完整工具链的大镜像，最终产物只拷编译结果到运行镜像，体积可能从 1G 降到 100M）；<b>不要用 latest 标签</b>（版本漂移会让\"昨天能跑今天不能跑\"）。</p><p>还有安全底线：容器默认不该以 root 运行，构建时创建普通用户并切换；不要把密钥、令牌写进镜像或作为构建参数——镜像层可以被任何拿到镜像的人解出来。</p><div class=\"ex\">一个 Java 服务的两阶段构建：第一阶段用完整 JDK 镜像编译打包，第二阶段只用精简运行环境加一个 jar 文件，最终镜像从 900MB 降到 120MB，拉取时间从几分钟变成十几秒。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别把密码、token 写进 Dockerfile 或镜像里，哪怕后面删掉也没用——镜像层是可回溯的，删掉只是在新层标记删除，旧层里还留着。敏感信息要用运行时注入或密钥管理服务。</div>","quiz":[{"type":"choice","q":"多阶段构建的主要收益是？","options":["构建更快","最终镜像更小、运行时更干净","更安全","不用写 Dockerfile"],"answer":1,"explain":"把编译工具链留在构建阶段，只把产物带进运行镜像，体积与攻击面都下降。","source":"想一想"}],"links":"<p>镜像做好之后放哪？→ <a href=\"#/l/container/05-registry\">下一课：镜像仓库</a></p>"});
