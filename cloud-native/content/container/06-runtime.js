/* container/06-runtime (自动生成) */
CCN.registerLesson({"id":"container/06-runtime","module":"container","order":6,"title":"容器运行时：真正干活的那一层","minutes":6,"keywords":["containerd","运行时","cri"],"concept":"<p>一句话：<gd data-term=\"container-runtime\">容器运行时</gd>是真正负责拉镜像、建命名空间、限资源、启动进程的底层程序，如 containerd、CRI-O；我们敲的命令只是它的外壳。</p>","core":"<p>层次大致是：命令行工具（docker、nerdctl）→ 高层运行时（containerd）→ 底层运行时（runc 之类，真正调用内核创建容器）。Kubernetes 通过 CRI 这个标准接口与运行时对接，所以集群可以用 containerd，也可以用别的实现，而不必绑定某一家。</p><p>理解这一层对排障很有用：容器起不来时错误来自哪个环节很重要——拉镜像失败是仓库与网络问题，创建命名空间失败是权限或内核参数问题，进程退出是应用自身问题。知道链路，才能定位到正确的日志。</p><div class=\"ex\">Kubernetes 从某个版本后不再内置 docker 作为运行时，很多团队遇到\"节点升级后容器起不来\"，根因就是运行时换了、镜像存储与命名空间都变了，需要重新对齐配置。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别把\"容器运行时\"和\"镜像格式\"混为一谈。运行时可以换，镜像格式是标准（OCI）。正因为有这两个标准，容器生态才能既不锁定厂商、又保持互操作。</div>","quiz":[{"type":"choice","q":"Kubernetes 通过什么标准接口对接不同的容器运行时？","options":["OCI","CRI","HTTP","SSH"],"answer":1,"explain":"CRI 是容器运行时接口，让编排层与具体运行时解耦。","source":"想一想"}],"links":"<p>绕了一圈，回到最实际的选型问题。→ <a href=\"#/l/container/07-container-vs-vm\">下一课：容器 vs 虚拟机</a></p>"});
