/* container/03-image-layer (自动生成) */
CCN.registerLesson({"id":"container/03-image-layer","module":"container","order":3,"title":"镜像与分层","minutes":6,"keywords":["镜像分层","只读层","可写层","复用"],"concept":"<p>一句话：<gd data-term=\"image-layer\">镜像</gd>由一层层只读层叠加而成，容器启动时在最上面加一层可写层——公共部分被复用，所以镜像能做得又小又快。</p>","core":"<p>一个典型镜像自下而上是：基础系统层（如精简的 Linux）、运行时层（JDK 或 Python）、依赖层、应用代码层。每层只记录与上一层的差异，多台机器、多个镜像之间可以共享相同的底层，本地存储与传输量都大幅减少。容器运行时只把最上面的可写层交给容器，下面全部只读共享。</p><p>这个设计带来两个重要推论：一是<b>层次顺序影响构建速度</b>——变化少的层放下面，改动频繁的代码放上面，能最大化命中缓存；二是容器<b>删除重建后数据会丢</b>，因为可写层随容器生命周期消失，持久数据必须挂载外部存储。</p><div class=\"ex\">把依赖安装与应用代码分成两层：改代码时只重建最后一层，几秒钟完成；如果写成一行同时装依赖和拷代码，每次改代码都要重装依赖，构建从几秒变成几分钟。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别在镜像里写入运行时数据。日志、上传文件、临时数据库都写进可写层的话，容器一重启全没了，还会把镜像层撑大。这些内容要么进日志系统，要么挂持久卷或对象存储。</div>","quiz":[{"type":"choice","q":"镜像分层的最大好处是？","options":["降低安全性要求","层可复用、构建与分发都更快更省","让容器跑得更快","减少操作系统依赖"],"answer":1,"explain":"共享只读层让存储与传输成本大幅下降，也让缓存命中成为可能。","source":"想一想"}],"links":"<p>分层是怎么被写出来的？→ <a href=\"#/l/container/04-dockerfile\">下一课：Dockerfile</a></p>"});
