/* container/02-isolation (自动生成) */
CCN.registerLesson({"id":"container/02-isolation","module":"container","order":2,"title":"namespace 与 cgroup：隔离的真相","minutes":6,"keywords":["namespace","cgroup","隔离","限额"],"concept":"<p>一句话：<gd data-term=\"ns-cgroup\">容器隔离</gd>不靠虚拟化，靠 Linux 内核两样东西——namespace 让进程\"看不见别人\"，cgroup 让进程\"用不过额度\"。</p>","core":"<p><b>namespace</b> 是一层视野隔离：进程、网络、文件系统挂载点、主机名、用户 ID、进程间通信都有各自的命名空间。容器里的进程看到的是自己那套 PID 编号（1 号进程就是应用本身）、自己的网卡与 IP、自己的根目录，看不到宿主机上的其他进程。<b>cgroup</b> 则是资源限额：限制能用多少 CPU、多少内存、多少磁盘 IO，超了就排队或被终止（内存超限常见表现是被 OOM 杀掉）。</p><p>理解这两点能解释很多现象：容器里 <code>top</code> 显示的 CPU 是宿主机的；容器内存超限被杀不是\"程序 bug\"而是配额；容器里改的 hosts 文件重启就没了。</p><div class=\"ex\">一个 Java 容器只给了 2G 内存限制，进程却按宿主机 64G 去算堆大小，结果频繁被 OOM 杀掉。修正是让 JVM 按容器可见的内存来配置堆——这是容器化最常见的坑之一。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别在容器里用\"按宿主机资源\"的方式调优。JVM、数据库、Node 等运行时会读系统总内存与核数，容器环境下必须显式告知限制值，否则会出现\"看起来配置很大、实际一直被限\"的怪象。</div>","quiz":[{"type":"choice","q":"容器内存超限通常会发生什么？","options":["自动扩容","进程被内核 OOM 终止","性能自动降级但不影响","宿主机重启"],"answer":1,"explain":"cgroup 达到内存上限时，内核会终止超额进程，表现为容器反复重启。","source":"想一想"}],"links":"<p>接下来看它怎么被打包出来的。→ <a href=\"#/l/container/03-image-layer\">下一课：镜像与分层</a></p>"});
