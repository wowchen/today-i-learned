/* database/04-index (自动生成) */
CCN.registerLesson({"id":"database/04-index","module":"database","order":4,"title":"索引与慢查询","minutes":6,"keywords":["索引","慢查询","执行计划"],"concept":"<p>一句话：<gd data-term=\"index\">索引</gd>像书的目录，让数据库能直接跳到目标位置而不是逐页翻——代价是占空间，并让每次写入都要顺带维护目录。</p>","core":"<p>没有索引时，按用户 ID 查订单要从头扫全表。索引把常用检索字段排序成便于二分查找的结构，查询从\"全表扫描\"变成\"定位若干页\"。但索引不是越多越好：每个索引都要在增删改时更新，表上十个索引会让写入明显变慢，占用的空间也可能超过数据本身。</p><p>排障的正确顺序是：先看<b>慢查询日志</b>找出耗时最长的语句，再看<b>执行计划</b>判断是走了索引还是扫了全表，最后才决定加索引或改写语句。常见无效情况包括：在索引列上做函数运算、类型不匹配导致隐式转换、以及<b>索引失效的最阴险形式——字段区分度太低</b>（比如只按\"性别\"建索引，筛选后仍剩一半数据）。</p><div class=\"ex\">一张千万级订单表原本按用户 ID 查询要 3 秒。分析执行计划发现是全表扫描，补上\"用户 ID + 创建时间\"的联合索引后降到十几毫秒——同一条 SQL，索引让结果从\"能不能用\"变成\"好不好用\"。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别在写查询时忽略数据量假设。小表上怎么写都快，一旦数据涨到千万级，当初随手写的模糊匹配、函数筛选、多表嵌套就会集中爆发。上线前用真实量级压一次，比事后加索引便宜得多。</div>","quiz":[{"type":"choice","q":"索引带来的主要代价是？","options":["查询变慢","占空间并拖慢写入，因为每次写都要维护索引","不支持事务","不能用于排序"],"answer":1,"explain":"索引是典型的\"读换写\"，所以要按查询模式精挑，而不是见字段就建。","source":"想一想"}],"links":"<p>关系型并不是唯一答案。→ <a href=\"#/l/database/05-nosql\">下一课：关系型之外的选择</a></p>"});
