/* k8s/03-pod (自动生成) */
CCN.registerLesson({"id":"k8s/03-pod","module":"k8s","order":3,"title":"Pod：最小调度单位","minutes":6,"keywords":["pod","副本","共享网络"],"concept":"<p>一句话：<gd data-term=\"pod\">Pod</gd>是 Kubernetes 里最小的调度单位，可以装一个或多个共享网络与存储的容器，通常一个 Pod 放一个主容器。</p>","core":"<p>为什么不直接调度容器？因为有些容器需要\"贴在一起\"：日志采集边车、代理边车、初始化容器，它们必须共享网络命名空间与卷，能被同时调度、同时迁移。同一个 Pod 内的容器可以用 localhost 互相访问，端口不能冲突。</p><p>Pod 是<b>一次性的</b>：它坏了不会\"修复\"，而是被删掉重建；重建后 IP 会变、名字会变。所以 Pod 不适合直接暴露给调用方，也不该用来做长久标识——它更像\"一批副本中的一个实例\"。需要额外能力时，通常加边车容器而不是把多个服务塞进一个容器。</p><div class=\"ex\">常见组合：一个主容器跑业务，一个边车容器负责把日志转发到日志系统，还有一个初始化容器在启动前把配置文件准备好。三者同生命周期、同网络视图。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别把\"多个服务塞进一个 Pod\"当省事。它们会被一起调度、一起伸缩、一起重启，任何一个的故障都会拖垮整组，还会让扩缩容粒度失真——拆成独立工作负载才是正解。</div>","quiz":[{"type":"choice","q":"同一个 Pod 内的多个容器共享什么？","options":["文件系统全部内容与进程空间","网络命名空间与部分存储卷","CPU 配额与镜像","没有任何共享"],"answer":1,"explain":"共享网络与卷是关键：可以用 localhost 互通，但文件系统与进程空间仍相互独立。","source":"想一想"}],"links":"<p>Pod 自己不会凭空出现，得有人声明它。→ <a href=\"#/l/k8s/04-deployment\">下一课：Deployment</a></p>"});
