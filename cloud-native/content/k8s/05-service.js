/* k8s/05-service (自动生成) */
CCN.registerLesson({"id":"k8s/05-service","module":"k8s","order":5,"title":"Service 与 Ingress：稳定的入口","minutes":7,"keywords":["service","ingress","负载均衡","固定入口"],"concept":"<p>一句话：<gd data-term=\"service\">Service</gd>给一组随时增减的 Pod 提供固定访问地址与负载均衡；<gd data-term=\"ingress\">Ingress</gd>在集群边界按域名与路径把外部请求转发进来。</p>","core":"<p>Pod 的 IP 会随重建而变，直接调用它等于把地址写死在代码里。Service 用一个稳定的虚拟地址加一组标签选择器，把流量分给当前所有匹配的 Pod；后端实例变化时，转发目标自动跟着更新。集群内部调用服务用这个名字就够了。</p><p>外部访问则分两层：四层用负载均衡类型的 Service 直接暴露，或者用 Ingress 做七层入口——按域名和路径路由（api.example.com 走订单服务、/static 走静态资源）、集中配置证书与限流。它是集群的\"前台\"，好处是所有对外规则集中在一处管理，而不是散落在每个服务里。</p><div class=\"ex\">典型做法：内部服务之间只用集群内地址互调，不暴露到集群外；只有 Ingress 对公网开放，统一挂证书、统一做限流。攻击面小、规则集中。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别把内部服务用公网可达的方式暴露出去。很多事故的起点就是\"为了方便调试\"给某个服务开了公网负载均衡，结果没有任何鉴权地被扫描到。内外网入口要严格分开。</div>","quiz":[{"type":"choice","q":"为什么调用方不应该直接指向 Pod 的 IP？","options":["Pod IP 不合法","Pod 是临时的，重建后 IP 会变","Pod 不能联网","这样不安全"],"answer":1,"explain":"Pod 随时被替换，Service 提供的稳定入口才是正确的调用方式。","source":"想一想"}],"links":"<p>配置和密码塞在镜像里不好，那放哪？→ <a href=\"#/l/k8s/06-config-secret\">下一课：配置与密钥管理</a></p>"});
