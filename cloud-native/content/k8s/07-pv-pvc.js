/* k8s/07-pv-pvc (自动生成) */
CCN.registerLesson({"id":"k8s/07-pv-pvc","module":"k8s","order":7,"title":"存储卷与持久化","minutes":6,"keywords":["pv","pvc","持久化","有状态"],"concept":"<p>一句话：<gd data-term=\"pv-pvc\">PV 与 PVC</gd>把存储的供给与申请分开——管理员准备存储（PV），应用按需申领（PVC），Pod 销毁重建后数据还在。</p>","core":"<p>为什么要有申请这一层？因为应用开发者不该关心底层是哪种云盘、在哪个可用区，只要说\"我要 100G 可读写存储\"；存储管理员则负责把它对上。这层抽象还让同一份清单能适配不同环境：云上自动创建云盘，测试环境用本地目录，接口不变。</p><p>访问模式是常被忽略的细节：单节点读写最常用，多节点同时读写需要支持共享的文件存储或对象存储，并不是所有后端都支持。有状态服务（数据库、消息队列）上云原生时，稳定性、数据一致性、备份方案必须一并设计，这也是\"有状态服务比无状态难得多\"的原因。</p><div class=\"ex\">一个数据库容器的清单里申领 200G 存储，Pod 被调度到其他节点重建时，存储被自动挂到新节点上，数据依然在——靠的就是这层解耦的存储抽象。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别指望\"容器重建数据自动找回\"是默认行为。没挂持久卷的话，数据就写在了容器可写层，容器一删全没。有状态服务上云前，先确认：存储后端是什么、能不能跟随调度、备份怎么做。</div>","quiz":[{"type":"choice","q":"PVC 的作用是？","options":["实际存放数据","应用对存储的需求声明，由系统匹配到具体存储","压缩数据","加密数据"],"answer":1,"explain":"PVC 是\"申请\"，PV 是\"供给\"，两者靠绑定完成对接，让应用与底层存储解耦。","source":"想一想"}],"links":"<p>最后一课看它怎么决定\"跑得好不好、够不够\"。→ <a href=\"#/l/k8s/08-probe-hpa\">下一课：探针、调度与自动扩缩</a></p>"});
