/* cloudsec/02-iam (自动生成) */
CCN.registerLesson({"id":"cloudsec/02-iam","module":"cloudsec","order":2,"title":"IAM 与最小权限","minutes":6,"keywords":["iam","最小权限","多因素认证","角色"],"concept":"<p>一句话：<gd data-term=\"iam\">IAM</gd>回答\"谁能对哪些资源做什么\"，核心原则是<gd data-term=\"least-privilege\">最小权限</gd>——只给刚好够用的权限，绝不给管理员。</p>","core":"<p>云上权限体系通常包含：用户与角色（谁）、策略（能做什么）、资源范围（对哪些对象生效）、条件（从哪个 IP、什么时间）。最常见的坏做法是给应用或同事配\"管理员全权限\"，理由是\"省事、先跑通\"。一旦凭据泄露，攻击者拿到的是整个账号的控制权。</p><p>正确姿势是：给应用分配<b>角色</b>而不是长期密钥（云上的实例可以通过角色获得临时凭据，不需要在代码里放密码）；按用途拆分策略，只允许必要动作（读某个桶、写某张表）；开启<b>多因素认证</b>，尤其是控制台登录；用<b>临时凭据</b>替代永久令牌，并定期审计权限使用情况。</p><div class=\"ex\">一次权限整改：把 12 个应用的\"管理员\"权限逐个替换为按资源与动作限制的策略，同时把硬编码的密钥改为实例角色获取临时凭据。整改后同样的凭据泄露场景，攻击面从\"整个账号\"缩小到\"某一个桶的读权限\"。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别长期保留\"先给全权限、以后再收\"的临时方案。临时权限没有截止日期就会永久存在，而权限膨胀几乎不会自动恢复。做法是配好时间限制与到期提醒。</div>","quiz":[{"type":"choice","q":"给应用授权时最推荐的方式是？","options":["一个账号全体应用共用","分配最小权限的角色并获取临时凭据","把密钥写进代码","所有应用都给管理员"],"answer":1,"explain":"最小权限加临时凭据，能在泄露时把影响限制到很小。","source":"想一想"}],"links":"<p>权限管住了，网络边界也要收。→ <a href=\"#/l/cloudsec/03-network-isolation\">下一课：网络隔离与边界防护</a></p>"});
