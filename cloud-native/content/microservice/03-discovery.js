/* microservice/03-discovery (自动生成) */
CCN.registerLesson({"id":"microservice/03-discovery","module":"microservice","order":3,"title":"服务发现：谁在哪","minutes":6,"keywords":["服务发现","注册中心","健康检查"],"concept":"<p>一句话：<gd data-term=\"service-discovery\">服务发现</gd>让服务实例启动时自动登记、下线时自动摘除，调用方按服务名查地址，不用手工维护 IP 清单。</p>","core":"<p>在容器环境里，实例的 IP 与数量随时在变，把地址写进配置文件根本活不下去。服务发现通常由注册中心承担：实例启动后向注册中心登记\"我是订单服务的某个实例，地址是 X\"，注册中心定期健康检查，调用方查询服务名得到可用实例列表，再选一个发起调用。整个过程自动完成，扩容缩容对调用方透明。</p><p>Kubernetes 环境里这一步通常由平台内置能力完成——Service 与集群 DNS 让\"按服务名访问\"成为默认行为，不需要额外引注册中心。非容器的老系统则常用专门的注册中心组件。区别只是实现，原理都是\"动态名册 + 健康检查\"。</p><div class=\"ex\">订单服务从 4 个副本扩到 10 个副本，调用方不需要任何配置变更就能把流量分到新实例；某个实例崩溃被摘除后也不再收到请求——这就是\"名册自动更新\"的价值。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别在客户端缓存服务地址太久。长时间不刷新会持续往已下线的实例发请求，表现为间歇性超时。客户端要么定期刷新，要么交给平台层统一处理。</div>","quiz":[{"type":"choice","q":"服务发现要解决的核心问题是？","options":["加密通信","实例地址动态变化时调用方仍能找到可用实例","限制流量","备份数据"],"answer":1,"explain":"名册随实例变化自动更新，调用方只认服务名，这是本质。","source":"想一想"}],"links":"<p>所有请求都直连服务合适吗？→ <a href=\"#/l/microservice/04-gateway\">下一课：API 网关</a></p>"});
