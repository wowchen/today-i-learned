/* microservice/04-gateway (自动生成) */
CCN.registerLesson({"id":"microservice/04-gateway","module":"microservice","order":4,"title":"API 网关：统一入口","minutes":6,"keywords":["api网关","鉴权","限流","路由"],"concept":"<p>一句话：<gd data-term=\"api-gateway\">API 网关</gd>是所有外部请求的统一入口，把鉴权、限流、路由、日志这些横切关注点集中在一处，而不是让每个服务各写一遍。</p>","core":"<p>没有网关时，每个服务都要自己处理令牌校验、防刷限流、跨域、灰度路由，代码重复且版本不一，安全策略也很难统一。有了网关，外部请求先到它这里：核对身份、判断有没有超限、按路径转发给对应服务，然后把结果返回。服务只关心业务逻辑。</p><p>网关也是<b>内外边界</b>：只有它对外暴露，内部服务只在集群内可达，攻击面显著缩小。不过它天然是流量的必经之路，所以它自身的高可用与性能必须过硬——网关故障等于全站故障，通常要部署多副本并做容量评估。</p><div class=\"ex\">一次安全整改：把所有对外接口的鉴权从各服务里收拢到网关统一做，顺带加上了按用户与接口维度的限流。服务代码删掉大量重复逻辑，安全策略也终于能一处修改全局生效。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别把业务逻辑写进网关。网关应该是\"薄\"的通道，一旦在里面塞了业务规则，它就会变成新的单体，每次业务改动都要动网关、都要全站发布，把解耦成果又还了回去。</div>","quiz":[{"type":"choice","q":"把鉴权与限流放在网关的主要收益是？","options":["让网关更忙","横切能力集中管理、服务代码不重复、内外边界清晰","提高数据库性能","减少服务数量"],"answer":1,"explain":"统一入口让安全与治理策略一处生效，同时收窄对外暴露面。","source":"想一想"}],"links":"<p>配置怎么管才不会到处是副本？→ <a href=\"#/l/microservice/05-config\">下一课：配置中心</a></p>"});
