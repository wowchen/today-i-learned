/* microservice/02-split (自动生成) */
CCN.registerLesson({"id":"microservice/02-split","module":"microservice","order":2,"title":"怎么拆才不会拆坏","minutes":6,"keywords":["领域边界","拆分粒度","数据库"],"concept":"<p>一句话：按<b>业务领域</b>拆而不是按技术层次拆，且每个服务应该拥有自己的数据——按\"技术层\"拆出来的服务会互相纠缠，改一处要动全部。</p>","core":"<p>最常见的错误是按技术分层拆：一个专门操作数据库的服务、一个专门做业务逻辑的服务、一个专门做前端的服务。结果是任何需求都要同时改三层，网络调用翻倍，完全没有解耦。正确做法是按业务能力拆：订单、支付、库存、用户各自成服务，每个服务内部自己管数据库、自己管逻辑，对外只暴露接口。</p><p>第二个关键原则是<b>数据私有</b>：订单服务不直接读支付服务的库，只能调它的接口。共享数据库看起来省事，实际上把两个服务又焊在了一起，将来想替换任何一个都动不了。跨服务的数据需求要用接口、事件或数据同步来满足，这是拆分必须付的成本。</p><div class=\"ex\">反例很典型：三个服务共用一个数据库，某次给一张表加字段，三个服务的代码都得改、得同时发版，等于拆了但白拆。改成各自持有数据、通过接口协作后，改动的爆炸半径才真正缩小。</div>","pitfalls":"<div class=\"pit\"><b>别踩坑 </b>别为了\"服务小\"而拆得过细。粒度太细会让一次业务请求跨十几次网络调用，延迟与失败概率都上升，运维成本也翻倍。合理的粒度是\"一个团队能完整负责一个服务\"。</div>","quiz":[{"type":"choice","q":"微服务拆分中\"数据私有\"的含义是？","options":["每个服务都要用不同数据库产品","服务只能通过接口访问别人的数据，不直接读别人的库","数据要加密","数据必须分布式存储"],"answer":1,"explain":"数据私有是解耦的关键，共享库会让服务之间的边界形同虚设。","source":"想一想"}],"links":"<p>服务多了，调用方怎么找到它们？→ <a href=\"#/l/microservice/03-discovery\">下一课：服务发现</a></p>"});
