/* dl/04-backprop (自动生成) */
AIX.registerLesson({
  id:"dl/04-backprop", module:"dl", order:4,
  title:"反向传播", minutes:4,
  keywords:["反向传播","训练"],
  concept:"<p><gd data-term=\"backprop\">反向传播</gd>是训练神经网络的核心算法:<b>根据最终错了多少,从输出层往回一层层算出\"每个权重该为这个错误负多少责\",再据此微调</b>。</p>",
  core:"<p>配合<gd data-term=\"gradient-descent\">梯度下降</gd>:反向传播负责算\"每个参数往哪个方向调、调多少\",梯度下降负责\"照着调一小步\"。一前一后,反复成千上万轮,网络就越来越准。</p><p>它就像考完试<b>复盘错题</b>:不只看错没错,还倒推是哪一步、哪个环节出的问题,然后针对性改进。1986 年辛顿等人把它推广开,多层网络才真正训得动,连接主义就此复兴。</p>",
  pitfalls:"<div class=\"pit\"><b>别踩坑</b>反向传播只是个高效的\"算账\"方法(本质是链式求导),它不理解任务含义。它能让网络拟合数据,但不能保证学到的是\"正确的道理\"而非数据里的巧合。</div>",
  links:"<p>掌握了通用训练法,接着看为特定数据量身定制的结构。先看图像的王者:CNN。</p>"
});
